# Implementation log

## 2026-09-17 - Profile calorie and fluid goal helpers

- Added a locally calculated Profile goal-helper sheet for calories and fluids, reusing the existing goal persistence rather than creating a second goal system. The latest cached weight and its reading date prefill when available; all inputs remain editable and consequential category choices require explicit confirmation.
- Implemented the 2023 DRI adult EER coefficient table as pure tested TypeScript with switchable US/metric entry and exact bidirectional lb/kg, inch/cm, and US fl oz/mL conversions. Maintenance is not multiplied twice and workout calories are not automatically added.
- Replaced ambiguous exercise-frequency activity labels with the official whole-day examples, including their substantial time assumptions. Results compare all four activity-category estimates for the same profile and allow immediate recalculation, while reminding users that a stable observed weight/intake trend can be more useful than a population equation.
- Added maintain, modest gain, and selectable 0.5/1/1.5/2 lb-per-week loss previews. Goal weight plus timeframe can add a custom comparison within 0.1–2 lb/week; faster requested rates are shown as unsupported. App-generated loss targets are excluded for current or goal BMI below 18.5, and all results retain uncertainty language.
- Added a beverage-goal helper using the National Academies' approximate beverage components, keeping total water including food distinct. Manual/clinician-directed goals accept US fluid ounces or milliliters. Goal-helper saves merge into the canonical saved record, so accepting calories cannot round an existing milliliter goal and accepting fluids cannot alter calories.
- Documented evidence, versions, inputs, limitations, and product heuristics in `docs/GOAL_HELPER_EVIDENCE.md`. Lint, strict TypeScript, 133 app tests, 43 Edge tests, all 32 browser flows, and a production iOS/Hermes export pass.

## 2026-09-17 - Reusable recipes and fractional batch logging

- Added a Recipes surface to Food tracking. Users create recipes entirely inside that surface, assembling and editing exact ingredient amounts in a recipe-only draft before saving a name and serving yield. The completed recipe is later added to a meal as one concise entry; its ingredients are not inserted into the meal draft.
- Sequenced the Recipes and ingredient-editor native sheets on iOS instead of stacking two native modals. Add ingredient now dismisses the recipe sheet, presents the food editor, and restores the intact recipe draft when editing finishes.
- Included saved recipes in Find or add food search as their own result group. Search selection carries the recipe ID and recipe entry method through portion review and meal saving, bypasses ordinary label creation, and excludes recipes inside the recipe ingredient picker to enforce the no-nesting rule.
- Divided the full ingredient nutrition by the declared yield and reused the existing portion calculator. Recipe logging accepts decimal/simple/mixed fractions; selecting `recipe` supports inputs such as `1/4` of a batch, while a yield of one models a complete sandwich or similar item.
- Stored validated ingredient snapshots so later food-label edits cannot silently change a recipe. Optional nutrients remain unknown if any ingredient lacks them, and nested recipes are rejected. Archiving a recipe preserves previously logged meals.
- Added `food_recipes` to portable export and applied linked migration `202609170001_food_recipes.sql` with user-owned RLS plus a same-user recipe foreign key on nutrition history. Migration history is aligned and linked database lint reports no schema errors.
- Added four recipe-domain tests and an isolated browser flow covering two-label creation, four-serving yield, fraction input, exact scaled nutrition, recipe provenance, and no duplicate labels. Lint, strict TypeScript, 125 app tests, 43 Edge tests, all 31 browser flows, and the production iOS/Hermes export pass; reviewed synthetic recipe-list and amount screenshots.

## 2026-09-17 - Stop vital trend lines across long empty periods

- Limited the off-screen neighbor used for chart continuity to one selected-window duration. A D/W/M/6M/Y chart therefore keeps a nearby line flowing through the plot edge but leaves a visible reading isolated when the adjacent reading is farther away than that range.
- Added weight and paired blood-pressure regressions for a September 7 to September 17 gap in the weekly view, including the empty intermediate window.

## 2026-09-17 - Profile Settings, per-food AI choices, and accessibility pass

- Split Profile into Profile and Settings views. Appearance, Face ID, Apple Health, and portable data export now live in Settings; account identity and goals remain in Profile.
- Resynchronized the Device appearance choice against the current iPhone color scheme whenever the app returns to the foreground. Kept automatic native appearance configuration, adaptive iOS colors, and persisted explicit Light/Dark overrides; an already-installed binary needs replacement to receive the native configuration.
- Made reusable-label creation independent for every food in an AI estimate and extended the estimation prompt to infer typical absorbed cooking oil/fat unless the description indicates a dry or oil-free method. Removed package-serving totals from discovery and meal-draft cards while retaining them in the amount selector.
- Matched Water's dark-mode Quick Log icon tile to the other actions, removed the Track tab ring, and removed the milliliter conversion beneath the fluid-ounce total.
- Added semantic headings, radio/checked states, live-region feedback, default button roles, and automated accessible-name checks across visible primary-flow controls. Native VoiceOver, switch control, and extreme Dynamic Type behavior remain registered-iPhone validation items rather than automated certification claims.
- Verified lint, strict TypeScript, 119 app tests, 43 Edge tests, all 30 isolated browser flows, and a production iOS/Hermes export. Deployed the updated `estimate-meal` function to the linked Supabase project; no database migration or user-data write was needed.

## 2026-09-13 - Suppress completed reminder occurrences

- Changed reminder completions to distinguish a whole-day daily/weekly occurrence from an individual multiple-daily time. Completion after one multi-daily alert targets the latest due slot; completion before the first slot targets that first upcoming slot, and later same-day times remain active.
- Replaced indivisible repeating notification requests with a rolling chronological queue of up to 60 dated iOS notifications. The queue is rebuilt after reminder/completion changes and replenished whenever the signed-in app launches or returns to the foreground.
- Completing an occurrence also dismisses its matching delivered notification from Notification Center. Daily completion skips only that local day, and marking a still-upcoming occurrence incomplete schedules it again.
- Verified focused reminder behavior, lint, strict TypeScript, 113 app tests, 42 Edge tests, and the development iOS/Hermes bundle. Exact delivery and dismissal remain physical-iPhone checks.

## 2026-09-13 - Add side-by-side standalone and development iPhone variants

- Kept preview/production as **HealthApp** with `com.jalen.healthapp` and added **HealthApp Dev** with `com.jalen.healthapp.dev` for the development profile, allowing both binaries to remain installed on the same iPhone.
- Added a distinct `healthapp-dev` URL scheme, build-aware signup/recovery redirects, automatic development-variant selection in the Metro startup command, and documented all required Supabase redirect allow-list entries.
- Verified standalone and development Expo identities, Expo flag forwarding through the development startup wrapper, lint, strict TypeScript, 110 app tests, and 42 Edge tests. EAS signing/building and physical-device installation remain user-run steps.

## 2026-09-12 - History shortcuts and native workout edge scrolling

- Added post-save actions that take Food directly to Food History and lifting directly to Workout History.
- Strengthened long-list exercise reordering with a larger edge trigger, held-card overflow, and non-animated incremental scroll targets in the pinned draggable-list patch so iOS can keep scrolling while the finger remains at the top or bottom.
- Removed the previous-workout template lookup, interface, confirmation, mapping code, and test coverage at the user's request.
- Verified lint, strict TypeScript, 107 app tests, 42 Edge tests, post-save history navigation, held-card edge auto-scroll, clean `patch-package` reapplication, and the production iOS/Hermes bundle. Physical-iPhone drag feel remains a hands-on validation item.

## 2026-09-12 - Portable Profile data export

- Replaced the uncommitted Strava work with an on-device data export in Profile. The records action packages every user-owned cloud record plus local reminders, completions, drafts, HealthKit state, and cached vitals into category CSV files and one complete JSON document.
- Added a separate photo-inclusive action that streams the ZIP to cache and adds each private original JPEG without recompressing it. Profile displays the photo count and stored size before export; Apple sharing lets the user choose Mail, AirDrop, Save to Files, or another destination, and the temporary archive is deleted afterward.
- Added browser download support, archive/CSV tests, and Profile browser coverage for the generated ZIP filename. Direct email composition was avoided because provider attachment limits vary and photo archives can be large.
- Verified lint, strict TypeScript, 107 app tests, 42 Edge tests, the focused Profile export browser flow, and a production iOS/Hermes bundle. Native sharing and a large photo export remain physical-iPhone checks, and the added native sharing module requires a replacement development build.

- Summary and History now keep their existing foreground refresh behavior without presenting the native pull-to-refresh control whenever a tab opens. The spinner and content shift appear only after an intentional pull down.
- Corrected the continuous W/M Blood Pressure timeline to aggregate a complete calendar-day bucket before filtering it into the visible window. An individual day therefore retains its exact averaged reading while it moves across the graph instead of changing at either viewport edge. Added the regression test; lint, strict TypeScript, 83/83 mobile tests, and `git diff --check` pass.
- Preserved the true time coordinate of the adjacent off-screen line buckets and clipped only their rendering. This keeps the continuous trend line's visible angle stable while a user drags the timeline; the regression test brings the mobile suite to 84/84 passing tests.

## 2026-09-04 — Face ID Quick Log layering

- Fixed the root-level Quick Log form sheet remaining above the Face ID privacy gate after backgrounding. The root navigator now dismisses only `/create` when biometric lock begins, retaining the user's underlying tab; a focused regression test covers that route guard.
- Simplified the privacy gate to its Face ID action and password fallback. Aligned the Progress Photos header to the app's safe-area/tab offset, and added an inexpensive metadata-only indicator for Weight History entries that already have uploaded photos.
- Corrected the Progress Photos modal on Dynamic-Island devices: its header now applies the reliable root safe-area top inset directly rather than depending on a modal-scoped SafeAreaView inset; the close target also has a larger hit area.

## 2026-09-04 — Private progress photos and wrapped muscle totals

- Added tested storage-capacity error classification for Supabase's HTTP 402 quota restriction and legacy quota/full-storage messages. Weight logging and the gallery now tell the user to delete progress photos or free Supabase Storage instead of exposing an opaque provider error.
- Kept every Workout History set count attached to its muscle label with a non-breaking space, so phrases such as `3 Shoulders` wrap as one unit.
- Added progress-photo capture to Weight logging with separate camera and system-library actions, a local preview/removal step, and upload only after the weight sample has been queued and synced.
- Added a progress-photo icon to every Weight History entry. It opens the signed-in user's private archive in a full-screen, horizontally paged gallery with camera/library upload, anchored weight association, timestamp and position, and an in-place delete confirmation.
- Optimized long-term storage by stripping EXIF through JPEG re-encoding, limiting the longest edge to 1440 px, progressively compressing toward 900 KB, enforcing a 2 MB hard ceiling, using immutable UUID paths, and lazily rendering only nearby gallery images. The app and database enforce three progress photos per local calendar day.
- Applied `202609040002_progress_photos.sql` to HealthHub. It adds user-owned metadata with RLS, a private `progress-photos` bucket restricted to JPEG and 2 MB objects, user-folder storage policies, per-day slot uniqueness, and lookup indexes. Linked database lint reports no schema errors.
- Added Expo SDK 57-compatible image picker/manipulator dependencies and iOS camera/photo-library usage descriptions. A new EAS development build is required for registered-iPhone testing.
- Verified ESLint, strict TypeScript, 74/74 mobile tests, 16/16 Edge tests, Expo public/native configuration, the production web export, and `git diff --check`.

## 2026-09-04 — Profile save feedback and Face ID protection

- Reduced Profile's Face ID control to one iOS-style Enable/Disable row. The current-password field is no longer permanently visible; tapping **Enable** presents a compact modal, keeps validation errors in place, and closes only after successful enrollment.
- Added a dedicated, accessible goal-save result directly below **Save goals** and clear it as soon as a value changes, so success and errors stay attached to the form that produced them.
- Removed self-explanatory introductory copy across login, Food, Workout, History, Water, manual vitals, workout editing, reminders, AI Coach, and the Face ID privacy gate. Retained concise text for validation, missing data, permissions, source verification, password-reset security, and destructive confirmations.
- Reworked Profile's name editor around an explicit saved-value baseline. A persistent colored status now distinguishes saved, unsaved, missing-required-field, and error states; the primary action says **Saving...** during persistence, **Name saved** when unchanged, and re-enables as **Save name** immediately after a field changes.
- Added the Expo SDK 57-compatible `expo-local-authentication` module and iOS `NSFaceIDUsageDescription` config. Profile now exposes an availability-aware Face ID switch that requires a successful system authentication before the per-user device preference is enabled.
- Added an authenticated-shell privacy gate for Face ID-enabled accounts. Leaving/backgrounding locks the shell, foreground authentication waits until iOS is active, and canceled or failed prompts offer retry or a local-device sign-out followed by password or Face ID login.
- Replaced reusable-password storage with a password-free, server-revocable biometric device credential. Enrollment revalidates the password once, creates a random 256-bit secret, stores only its SHA-256 hash in the RLS-protected/server-only `biometric_device_credentials` table, and places the secret behind Face ID in the passcode-required, device-only iPhone Keychain. The public authentication action accepts only a correctly hashed secret; enrollment and revocation still validate a Supabase user JWT internally.
- Made active Supabase sessions process-only, so force-quitting returns to the login page and Face ID creates a fresh server session. Ordinary sign-out is now scoped to the current device rather than globally signing out every device. The login screen's accessible **Remember me** checkbox retains only email/account identity; turning it off during password login revokes and removes the matching Face ID enrollment.
- Added a database trigger that revokes every biometric device credential whenever `auth.users.encrypted_password` changes, covering recovery on this or another device. The local reset flow also removes its Keychain enrollment immediately and returns with an explicit new-password/re-enrollment notice; another device detects the server revocation on its next Face ID attempt, removes the stale credential, and asks for password sign-in. Existing pre-upgrade Keychain records containing a password are deleted rather than migrated, while their non-sensitive account identity can remain remembered.
- Replaced the oversized Face ID text controls with Expo SDK 57's native `faceid` SF Symbol: login uses a compact account row, Profile uses an icon-led settings row and setup/disable action, and the privacy gate uses an icon-only 58-point unlock target with a small caption. VoiceOver labels and the existing password fallback remain intact.
- Applied `202609040001_biometric_device_credentials.sql`, deployed ACTIVE `biometric-auth` version 1 with internal JWT handling, confirmed an unknown credential receives HTTP 401, aligned remote migration history, and found no linked-schema lint errors. The full lint, strict TypeScript, 69/69 mobile-test, and 16/16 Edge-test suite passes. Expo Go cannot test iOS Face ID or the native SF Symbol presentation, so a new EAS development build and registered-iPhone validation remain required.

## 2026-09-02 — Tracking expansion and editable manual history

- Deduplicated food amount text when the serving label already begins with the same amount and unit, so History and the current meal show `1 bottle (14 fl oz)` instead of `1 bottle · 1 bottle (14 fl oz)` without product-specific rules.
- Removed the Open Food Facts/My label/Manual source badge from Food History cards while retaining source metadata internally.
- Required first and last name during account creation and stored them in both Supabase Auth metadata and `public.profiles` via applied migration `202609020003_profile_names.sql`. Hosted duplicate-signup responses with an empty identity list now show an existing-account error instead of navigating to false success.
- Completed password recovery by parsing native implicit-token and PKCE callbacks, establishing the recovery session, and adding the previously missing new-password route. Reset confirmation wording now reflects Supabase's non-enumerating response and retry interval.
- Personalized the Summary header as `Hi FirstName,` with `Your daily snapshot` beneath, and suppressed the redundant no-change Apple Health sync success message. Changed Weight's top-right trend value from the latest reading to the current displayed-range average and reduced its subtitle to the same range-average label used by Blood pressure.
- Added first- and last-name fields plus a Save name action to Profile's Account card for pre-existing accounts. The editor loads public profile values with an Auth-metadata fallback and writes both stores so Summary immediately uses the saved first name.
- Fixed reusable scanned-food corrections appearing not to save: edits now preserve every serving conversion, including weight and volume, and future scans load a matching user-corrected/private barcode profile before displaying the shared Open Food Facts result. Confirming an adjusted scan updates the existing profile instead of skipping persistence, and the corrected marker remains durable across later edits.
- Clarified the food-label form by separating the optional human-readable serving label from optional structured logging conversions. Item amount/unit powers bottle, package, bar, and piece choices; weight and volume independently expose their matching amount units and remain blank when the package does not provide them.
- Made food-label weight and volume unit chips convert the current input in place instead of merely relabeling it. Conversions use the same canonical gram/milliliter factors as nutrition calculations and produce readable values such as `113.4 g` to `4 oz` and `414 mL` to `14 fl oz`.
- Made every scanned or tracked food automatically create or reuse a private My Foods label. The amount editor now profiles unprofiled Recent foods, legacy Basic entries do the same, and final meal saving backfills any restored older draft that bypassed those paths.
- Extended label deduplication without conflating same-name products: catalog ID and canonical barcode remain the primary identities, while barcode-free labels are reused only when normalized name, brand, source, serving conversions, and every stored nutrient match. Re-tracking a deleted corrected barcode label safely restores the private correction instead of leaving it archived.
- Restricted unfinished-meal item editing to the consumed amount, unit, and note. The editor now opens every legacy/current draft item directly in **Edit amount**, uses **Save amount**, and closes directly back to the meal; Back can no longer enter label/scanner creation and overwrite the entry being edited.
- Added a dedicated **Manage labels** control beside **Add food** on the Food screen. Its focused My Foods list is the only logging surface that exposes Edit label and Delete label, keeping reusable serving/nutrition corrections separate from routine food selection.
- Changed repeat barcode scanning to query the user's normalized private barcode label before calling Open Food Facts. Any existing profile, corrected or not, is reused immediately by profile ID, preventing duplicate creation and ensuring saved accuracy corrections cannot be overwritten by provider refreshes.
- Removed Corrected, Open Food Facts, My label, Manual, and Recent source badges from the unfinished-meal cards, food suggestions, and amount editor while retaining source/correction metadata internally and keeping the package-verification notice in the scan confirmation form.
- Removed the user-editable free-text serving label and its redundant read-only preview from food-label creation and management. One structured Serving size section now supplies item, weight, and volume inputs, while the app generates labels such as `1 package (49.6 g)`, `12 pieces (28 g)`, and `1 bottle (14 fl oz)` internally wherever display text is needed. Existing/provider label text remains an internal fallback only when no structured conversion can generate a display value.
- Required every new or edited label to have a reproducible serving basis: positive weight, positive volume, or positive count with a specific physical item unit. Generic `serving`/`portion` values and mass or volume names entered in the item-unit field are rejected; scans and older saved barcode labels without a valid basis remain on confirmation with package-specific guidance.
- Existing labels infer a sensible editable measurement unit from their stored display string before converting canonical grams/milliliters for the form, preserving familiar values such as `4 oz` and `14 fl oz` instead of reopening them as long decimal base-unit values.
- Added a **View daily totals** action to every Food History date. Its in-app popup totals calories, protein, water/fluids, carbohydrates, fat, fiber, sodium, and sugar. Optional nutrients show `recorded` or `Not available` when older/basic entries make a complete daily total impossible.
- Restored the unified **Find or add food** flow after it was accidentally reverted: the same screen searches Recent/My Foods, uses a selected saved match, and offers a reusable label prefilled from the query only when no exact saved food name exists. Basic Entry is no longer a separate new-food method; legacy unfinished basic entries remain editable.
- Added daily water/fluid totals to Food History, including hydration-only dates, using the same local-day grouping as food history.
- Renamed the user-facing Exercise destination and History tab to Workout. Workout History now shows an explicit total for each exercise and a session-level set breakdown by muscle group.
- Added a per-exercise muscle-group selection for multi-muscle lifting sessions and workout editing. Applied `202609020002_workout_muscle_group_sets.sql`; existing single-muscle sessions backfill automatically, while legacy multi-muscle sets remain visibly unassigned instead of being guessed.
- Added a bounded, accessible vertical drag handle to the lifting logger, repeated Add exercise below long workout lists, and persisted an optional gym/location value with each workout. Workout History editing retains explicit accessible up/down arrow buttons for precise reordering.
- Restored compact Workout History set text such as `3 x 12, 12, 12 at 45 lb` and changed the muscle breakdown to count-first wording such as `5 Back · 3 Bicep · 3 Shoulders`.
- Replaced the per-exercise set-count badge with its muscle group, expanded the user-facing Bi/Tri/Delt labels to Bicep/Tricep/Shoulders without rewriting stored history, and ordered History as Workout/Food/Blood pressure/Weight.
- Moved each food timestamp beside its name in smaller text. Food History editing now changes the consumed amount and unit, reconstructs the historical per-serving basis, and recalculates its nutrition totals instead of exposing nutrition-fact fields.
- Made every manual History editor return explicitly to the relevant History category after save or after tapping its custom History back control, rather than falling through to Summary. Apple Health/imported records remain read-only.
- Added one tested blood-pressure classifier for Normal, Elevated, Stage 1 Hypertension, and Stage 2 Hypertension. History shows a readable colored category badge while retaining standard dark text for the reading itself; the Summary BP value and chart use the same classification.
- Updated the BP chart with category-colored paired markers, solid systolic points, outlined diastolic points, a category key, and colored range/selected readings. Both vital charts now support horizontal swipes through dated D/W/M/6M/Y windows, including empty periods; D retains intraday points, W/M use daily points, 6M uses weekly points, and Y uses monthly points.
- Hardened chart gesture arbitration by capturing only deliberate horizontal movement, refusing responder termination mid-swipe, and disabling the authenticated shell's competing iOS dismiss gesture. Vertical Summary scrolling and chart-point taps remain available.
- Replaced release-triggered chart jumps with a three-window animated timeline. The previous/current/next dates, axes, and data translate with the finger, committed drags settle into the adjacent period, short drags spring back, and the current window applies resistance toward unavailable future dates.
- Connected both chart responders to Summary's vertical `ScrollView`: recognizing a horizontal chart drag disables vertical touch-scrolling for the remainder of that gesture, and release, cancellation, or chart unmount reliably restores it. Directional locking remains enabled for ordinary vertical page gestures.
- Removed the remaining page-panel interaction from vital charts. Horizontal movement now updates the real rolling-window timestamps, axes, averages, and plotted series continuously during the gesture; release performs no transition and preserves the exact position. The live present remains the sole movement boundary.
- Reduced chart clutter by averaging both weight and paired blood-pressure readings into local hour buckets for D, calendar days for W/M, calendar weeks for 6M, and calendar months for Y. Added timestamp-proportional axis divisions every 3 hours, day, 7 days, month, and 2 months respectively, and enlarged the plot vertically and toward the card edges.
- Stabilized each chart's vertical scale from all loaded readings for that vital rather than the currently visible window. Weight uses all loaded weight values, while blood pressure shares one domain across all loaded systolic and diastolic values; scrolling therefore cannot make either Y-axis jump, and no fixed medical or product-specific values are hardcoded.
- Extended each visible weight, systolic, and diastolic stroke through the nearest averaged bucket immediately outside both window edges. The SVG clips those connection-only points to the plot, while markers, tap targets, empty-state detection, and displayed averages remain limited to readings inside the selected rolling window.
- Replaced timestamp-based matching between independently averaged BP series with one bucketed systolic/diastolic pair. Marker and selected-reading colors now classify that exact pair, even when original systolic and diastolic timestamps differ slightly.
- Changed 6M aggregation from monthly to Sunday-through-Saturday averages while retaining monthly axis divisions; Y remains monthly. Replaced the large below-chart inspector with a content-fitted box anchored above the selected point. Every range uses a balanced nine-unit horizontal margin instead of a fixed width that leaves excess whitespace or a margin that crowds the text. Its labels show an hourly span for D, a date for W/M, a week span for 6M, or a month for Y followed by AVG and the reading. Removed the systolic/diastolic legend while retaining the BP category-color legend.
- Anchored aggregate timestamps to their actual bucket boundaries instead of the mean clock time of their source readings: D to the hour, W/M to local midnight, 6M to Sunday, and Y to the first of the month. Weekly points now land exactly on their matching daily X-axis divisions, and edge-connection points exclude duplicate partial buckets.
- Moved the `mmHg` unit to the main BP average and removed it from BP point tooltips and the redundant average subtitle; weight unit presentation is unchanged.
- Removed the persistent point-tap and horizontal-drag instructional messages beneath both vital charts while preserving those interactions and the empty-period message.
- Moved Sync from the Trends heading to the Summary header's top-right corner, added an accessible refresh icon and pressed/disabled states, and kept the last-sync timestamp directly beneath the control.
- Removed the explanatory text from the calorie calendar, added deliberate spacing between its header and weekday row, and title-cased the heading as in `September 2026 Calories`.
- Standardized Summary widget targets with a `Goal:` prefix for Weight, Blood pressure, Calories, Protein, and Water while retaining available Weight/BP measurement timestamps.
- Added Edit and confirmed Delete actions to user-created food labels in Recent/My Foods. Edits update the reusable profile, while deletion archives it and leaves immutable historical meal snapshots unchanged.
- Moved saved-food label deletion confirmation out of the scroll content into a centered, dimmed overlay above the current screen. Opening it dismisses the keyboard, and Cancel or the device back action closes the confirmation without navigating away.
- Prevented long Workout History muscle summaries from pushing session set totals against or beyond the right card edge by reserving a fixed, right-aligned total column. Matched the Food History water total to the calorie/protein type treatment and added a 40 fl oz quick-water shortcut.
- Added user-owned hydration storage and logging for water/other fluids in mL, fl oz, and cups. Profile now stores a daily water goal, and Summary renders today's fluid-ounce progress.
- Reordered the Track sheet into the requested two-column Food/Water, Blood Pressure/Weight, Exercise/Reminders layout.
- Added a calorie calendar using green progress rings at/under the daily goal and red rings over it. The calendar can navigate backward and forward across historical months, reloads on Summary focus, and prevents future-month navigation. Calories and Protein open Food History, and the former lift/cardio-today strip was removed.
- Anchored Workout History muscle-group badges to the top-right of each exercise row and prevented badge shrinking, so long exercise names can wrap without displacing the identifier.
- Added edit actions for manual cardio, weight, blood pressure, and food history. Workout editing retains exercise order and location. HealthKit/imported health records and imported food records remain read-only.
- Applied `202609020001_tracking_expansion.sql` to HealthHub. Remote migration history is aligned and database lint reports no schema errors.
- Verified ESLint, strict TypeScript, 65/65 mobile tests, 14/14 Edge tests, and `git diff --check`. The production web export passed before the final live-drag refactor; its follow-up Metro export stalled while bundling and the two export-only Node processes were stopped, so the final gesture behavior remains an iPhone/Metro validation item.

## 2026-08-29 — Releases 0–1 implemented locally

- Added `AGENTS.md`, roadmap, status, implementation log, ADR, and hosted Supabase setup guide for continuous project context.
- Scaffolded Expo SDK 57 with Expo Router, strict TypeScript, ESLint, and Node test runner.
- Implemented secure Supabase session persistence, email/password auth flows, and a three-tab mobile shell.
- Implemented local-first vital caching, idempotent SQLite outbox, manual weight/BP/pulse tracking, sync, and trends.
- Added `profiles` and `vital_samples` migration with explicit RLS policies.
- Passed `npm run typecheck --workspace=@healthapp/mobile`, tests (2/2), and lint.
- Passed Expo Doctor (21/21 checks) after deduplicating React.
- Configured the supplied public Supabase project URL and publishable key in ignored `apps/mobile/.env`.
- Applied the Release 1 migration to the hosted development project; added Expo's SDK-compatible web runtime after Metro reported a missing `react-native-web` module. `npm run check` passes.
- Added an explicit web and unavailable-bridge fallback from SecureStore to AsyncStorage; native devices continue to use SecureStore when available.
- Fixed the post-auth navigation path so successful sign-in enters the authenticated app instead of leaving the user on the sign-in screen.
- Replaced essential native alert-only actions with visible in-app save and sign-out feedback, including error messages and explicit logout navigation.
- Split local vitals storage by platform: native uses SQLite; web preview uses AsyncStorage to avoid Expo SQLite's alpha WebAssembly setup while preserving the same sync/outbox behavior.
- Verified the repaired browser path with `npx expo export --platform web`; the web bundle now completes successfully.
- Deferred only the remote migration application and two-user RLS verification until development Supabase credentials exist.

## 2026-08-29 — Training, cardio, nutrition, and dashboard core

- Applied `202608290002_training_nutrition.sql` to the linked hosted Supabase project. It creates workout sessions/sets, cardio entries, and nutrition entries with explicit per-user RLS.
- Added a clean lifting workflow with exercise search, editable sets/reps/weights, workout notes, and last-session exercise memory to support progressive overload.
- Added manual cardio logging for walking, running, swimming, tennis, cycling, and other activities.
- Added meal logging with calorie/protein targets and a searchable quick-food list.
- Extended Today with nutrition/activity summaries, a weight trend, and a single combined systolic/diastolic blood-pressure chart with averages.
- Added an all/manual source filter and retained source values from remote vital records, ready for later HealthKit and Omron imports.
- Reworked Lift to begin with a muscle group, then add/remove exercises. Each exercise has a set-count field that creates one rep field per set, a single working-weight field, and a `Previous: 3 x 12, 12, 12 at 135 lb`-style history line.
- Fixed Today tab freshness by reloading vital data whenever it regains focus after tracking a manual reading.
- Added the History tab for scrollable completed workouts, showing date, muscle group, exercise-level rep/set/weight summaries, and notes.
- Replaced simple name matching with ranked exercise suggestions. Selecting or finishing an exercise search fetches its strongest 90-day session, so an off day does not replace the displayed benchmark.
- Added a tested conservative double-progression rule: only flag a weight increase after at least two working sets at the strongest recent weight reached 12 or more reps; otherwise suggest building toward 12 reps first.
- Removed every seeded exercise and food suggestion. Both search fields now query only the signed-in user's prior completed entries; a newly saved exercise or food becomes selectable on the next search.
- Made muscle-group chips multi-select and applied `202608290003_workout_muscle_groups.sql` to the hosted project. New workouts retain their exact selected group list; legacy rows still display safely.
- Added local persistence and display for the last successful vitals sync, plus last-measured timestamps beside the displayed weight and blood-pressure readings.
- Removed the final preselected field (cardio activity), so every new workout, cardio, and food log begins with choices made by the user.
- Added D/W/M/6M/Y charts. D uses all readings from 12:00 AM through 11:59 PM with timestamp-proportional points and time labels; W/M/6M/Y use the newest reading per day.
- Added test coverage for daily trend behavior, bringing the suite to 6 passing tests.
- Added the History edit route and applied `202608290004_replace_workout_session.sql`. It validates ownership and replaces the session plus its sets in one database operation.
- Added Exercise, Blood pressure, and Weight history views. BP readings are paired by their shared correlation ID and all vital lists include time and source.
- Applied `202608290005_daily_goals.sql` and added persistent calorie, target-weight, and systolic/diastolic goal fields. Summary derives protein as `0.7 g/lb` from the latest weight and displays calorie/protein `current / goal` progress.
- Moved the manual cardio logger into Exercise, deleted the separate cardio route, and renamed navigation to Summary, Health Log, and Exercise.
- Applied `202608290006_bp_goal_defaults.sql`, which fills missing BP targets at 120/80 and sets those defaults for new profiles.
- Moved all editable goal controls from Summary to Profile; Summary now presents a read-only goal card and progress values.
- Updated the lifting entry and workout-history editor so 0 lb is a valid explicit working weight for bodyweight exercises; blank weight fields remain invalid. Re-verified with the full check suite (6/6 tests) and web export.
- Added `react-native-svg` for cross-platform charts and excluded generated Expo `dist` output from linting.
- Verified with `npm run check` (lint, strict TypeScript, 2/2 tests) and `npx expo export --platform web`.
- 2026-08-29: Collapsed the Cardio form on the Exercise tab behind a tap-to-add header. It can be reopened or hidden without losing the current form values, and the full check suite passes (6/6 tests).
- 2026-08-29: Applied `202608290007_daily_protein_goal.sql`. Summary cards now open their matching detail screen; Exercise History merges cardio and lifts by timestamp, and individual cardio cards have a confirmed delete action. Profile accepts an optional protein target and otherwise uses the automatic 0.7 g/lb target. Verified with the full check suite (6/6 tests) and a web export.
- 2026-08-29: Added confirmed deletion to every History entry: workouts, cardio, weight, and blood-pressure readings. Vital deletion uses a queued soft-delete tombstone so it remains correct offline and includes all values associated with a single BP measurement. Verified with the full check suite (6/6 tests) and a web export.
- 2026-08-29: Replaced platform alert dialogs with an in-app History deletion sheet, so confirmation is visible and usable in browser preview and mobile. Verified with the full check suite (6/6 tests) and a web export.

## 2026-08-30 — Reliability and mobile readability pass

- Fixed exercise-history retrieval so an explicit 0 lb bodyweight performance remains valid, names match without case sensitivity, and a stale blur/partial-name request cannot overwrite the selected saved exercise. Search now also matches punctuation variants such as `pullups` to `Pull-ups`.
- Rebuilt the weight and blood-pressure plots with labeled y-axis values, inclusive non-daily date labels, larger tappable points, and an exact reading/timestamp inspector. The W/M endpoint no longer displays the next day's exclusive midnight.
- Removed the duplicate Summary goals card, kept goal context in the top metrics, and made the six-item bottom tab bar safe-area aware with compact labels.
- Replaced alert-callback-dependent signup/reset success behavior with an explicit check-email screen and in-app authentication errors, so browser preview and mobile share the same route behavior.
- Simplified goal-save feedback to `Goals saved.`; the automatic protein explanation remains beside the setting where it is relevant.
- The user observed that a second account does not display the first account's data, confirming basic read isolation. Direct cross-user update/delete policy checks remain before the RLS roadmap gate is complete.
- Verified with `npm run check` (lint, strict TypeScript, 12/12 tests), `git diff --check`, and `npx expo export --platform web`.

## 2026-08-30 — EAS and Apple signing foundation

- Linked `@jalenwang/healthapp`, installed Expo dev client, configured development/preview/production EAS profiles, and registered the app as `com.jalen.healthapp`.
- Apple Developer enrollment, the managed distribution certificate, ad hoc provisioning profile, and the physical development iPhone are active. No credentials need to be regenerated for the retry.
- Diagnosed the pre-upload failure by reproducing EAS's exact shallow local clone: the apostrophe in `Jalen's Ultimate Health App` breaks Git-for-Windows quoting for the generated `file:///` clone URL.
- Validated Expo's supported no-VCS packaging workaround with `build:inspect`; it includes the workspace root lockfile and mobile EAS configuration while excluding the ignored mobile `.env`.
- Updated runtime configuration to accept EAS's `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY` while preserving support for the legacy anon-key variable name.

## 2026-08-30 — HealthKit read-only import foundation

- Standardized local and EAS Supabase variables on the canonical HealthHub project after diagnosing an accidental second-project override.
- Added `@kingstinct/react-native-healthkit` 14.1 with Nitro Modules, a read-only HealthKit entitlement, and a focused Apple privacy explanation. No HealthKit write or background-delivery access is requested.
- Added permission-gated, one-year foreground imports for body mass, paired blood pressure correlations, and supported cardio workouts. Strength workouts remain in Apple Health because they do not include the exercise/set detail required by the lifting model.
- Added local per-user HealthKit anchors, deterministic UUIDv5 record identities, imported-deletion handling, source app/device provenance, and tombstone preservation so sync is incremental and retry-safe.
- Applied `202608300001_healthkit_imports.sql` to HealthHub. It adds external IDs/source labels, HealthKit cardio support, durable cardio tombstones, and per-user uniqueness indexes under the existing RLS policies.
- Added an Apple Health card to Health Log, an Apple Health Summary filter, and source labels in History. Combined graphs deduplicate identical manual/imported readings within five minutes while source-specific history remains available.
- Pinned React 19.2.3 at both workspace levels after Expo Doctor found npm had hoisted React 19.2.8. Verification passes: lint, strict TypeScript, 16/16 tests, Expo Doctor 21/21, web export, Expo entitlement introspection, and a clean remote migration dry run.
- EAS enabled the HealthKit capability, regenerated the registered-iPhone provisioning profile, and successfully produced development build `96ed5eb2-ec55-4cc0-9c40-308e1b84d998`.
- During device validation, Metro reported a missing `NitroModules` runtime. The successful build's Xcode log confirms that `NitroModules` and `ReactNativeHealthkit` were both compiled and linked with `RCT_NEW_ARCH_ENABLED=1`, identifying an older installed client or Expo Go as the mismatch. Added a pre-import native-module guard and documented a clean installation of the exact EAS build.
- After the correct client reached HealthKit, the first Connect action terminated natively. Split permission authorization from data import so no query runs during the permission interaction, and serialized the three anchored reads instead of invoking them concurrently. This is a Metro-delivered TypeScript change and does not require another native build; a repeated native termination now has a precisely isolated Connect or Sync phase for crash-report analysis.
- The exported iOS 26.6.1 crash report localized the repeated termination to `CoreModule.requestAuthorization`, with `EXC_BREAKPOINT`/`SIGTRAP` while an Objective-C `NSException` was being raised. Verified the signed IPA contains `NSHealthShareUsageDescription`, then removed the blood-pressure correlation type from the permission set while retaining its systolic and diastolic quantity types. A regression test prevents reintroducing that interdependent authorization combination; correlation reads remain unchanged.

## 2026-08-30 — Combined sources, meal builder, and mobile navigation pass

- Removed the tab navigator's duplicate native headers and enabled automatic iOS scroll insets on the affected screens, eliminating the blank top area while preserving status-bar safety.
- Removed Summary's source selector. Weight and BP charts now always use the deduplicated combination of manual and Apple readings; History continues to expose source provenance per record.
- Split Exercise into Lifting and Cardio subsections so each logger has a focused surface without adding another bottom-navigation item.
- Replaced the single-food form with a meal-first, multi-food builder. Each row supports add/remove, search against only the current user's saved foods, and calorie/protein entry; one save gives the meal's items a shared timestamp.
- Added Food History grouped by local date and meal, including daily calorie/protein totals and confirmation-based deletion for each food item.
- Replaced timestamp-shaped fallback IDs with valid UUIDv4 generation on native and web runtimes, fixing PostgreSQL `invalid input syntax for type uuid` failures.
- Reduced Apple Health permissions and import queries to weight and paired BP only. Suppressed the bridge-only `SourceProxy` label while retaining meaningful source-app names.
- Verified lint, strict TypeScript, 18/18 tests, `git diff --check`, and a successful Expo web export.

## 2026-08-30 — Durable lifting drafts

- Added a validated, user-scoped AsyncStorage workout draft. Lifting changes save continuously, restore after tab navigation or an app restart, and are flushed again when the app becomes inactive.
- Incomplete drafts stay on the current device and do not enter Supabase or Exercise History. A successful Finish workout save clears the local draft only after the completed workout is written remotely.
- Updated every Food History entry to use the same bordered Delete button and existing in-app confirmation sheet as workout, cardio, weight, and BP history entries.
- Verified with `npm run check`, `git diff --check`, and 20/20 tests.

## 2026-08-30 — Returning-tab top inset correction

- Diagnosed the Summary and History top gap as iOS reapplying `ScrollView` automatic content insets after a tab regained focus. Both screens now explicitly use `contentInsetAdjustmentBehavior="never"` and a fixed Safe Area top inset, matching the other tabs on first load and after tab navigation without a duplicate header-sized gap.
- Set the shared tab scene background to the app surface color. This makes the safe-area/status-bar region consistent across Summary, History, Exercise, Health Log, Food, and Profile; loading data no longer changes its appearance.

## 2026-08-30 — Unified Summary and Apple Health sync

- Moved the one-time Apple Health connection and status card from Health Log to Profile. Health Log now focuses solely on manual vital entry.
- Summary now uses a single guarded sync path: on tab focus it syncs Supabase and, for a connected iPhone user, imports Apple Health weight/BP in the same operation. The existing **Sync now** control forces that identical combined operation.
- Removed the standalone Apple Health sync action so connected users do not need to run two different sync controls. A failure to import Apple Health still allows the ordinary Supabase vital sync to complete and reports a clear status.
- Verified `npm run check`, `git diff --check`, and a successful Expo web export.

## 2026-08-30 — Focused mobile creation flow

- Replaced the six-tab bar with Summary, a raised center Create control, History, and Profile. The center control presents a native Expo Router form sheet with Food, Health, Exercise, and Weight choices, then routes directly to the selected logger.
- Made Weight and Blood pressure distinct logging surfaces. Both continue to write the same user-owned `vital_samples` records and use the existing offline outbox/Supabase sync, so no new table or migration is needed.
- Verified lint, strict TypeScript, 21/21 tests, `git diff --check`, and a successful iOS bundle export.

## 2026-08-31 — Reusable nutrition and Open Food Facts barcode foundation

- Replaced the name-only nutrition form with Basic, Search saved foods, Create food label, and Scan barcode workflows while preserving meal-first multi-food logging.
- Added private brand-aware food profiles, immutable nutrition-entry snapshots, core label nutrients, exact serving/mass/volume conversion metadata, meal grouping IDs, RLS, and server-only catalog/cache tables in `202608310001_food_catalog.sql`.
- Applied the migration to HealthHub and deployed the authenticated `resolve-food-barcode` TypeScript Edge Function. It validates/canonicalizes UPC/EAN codes, caches results, identifies HealthApp to Open Food Facts, and returns editable normalized nutrition without exposing server credentials.
- Added exact g/oz/lb and US mL/fl oz/cup/tbsp/tsp calculations, runtime Zod validation, private provider corrections, brand/source badges, enhanced Food History, and user-scoped AsyncStorage meal drafts.
- Installed and configured the Expo SDK 57 camera module with a purpose-specific iOS permission message and manual barcode fallback for web.
- Added an explicit failed-lookup fallback that offers to create a private profile with the scanned barcode already attached instead of leaving the user at an error message.
- Preserved the existing uncommitted center-tab `Track` label change and its prior documentation update.
- Verified lint, strict TypeScript, 26/26 tests, Expo Doctor 21/21, `git diff --check`, web export, iOS/Hermes export, the remote migration list, and ACTIVE Edge Function deployment. A replacement EAS binary remains pending because adding the native camera module requires a new iOS development build.

## 2026-08-30 — Summary nutrition and trend refinement

- Replaced the text-only Summary calories/protein cards with compact circular progress rings that retain current and goal numbers and open Food when tapped.
- Changed the initial Summary trend range to weekly. Chart-point inspection now toggles: one tap opens the value/time panel and another tap on the same point closes it.
- Added a pure regression test for the point-selection toggle. Verified lint, strict TypeScript, 21/21 tests, and `git diff --check`.

## 2026-08-30 — Centered creation and future-care placeholders

- Reordered the five visible tabs to Summary, History, centered Create, AI Coach, and Profile. Reminders is available as a Create-sheet choice, which preserves the clean permanent navigation while keeping medication/supplement/BP prompt setup discoverable.
- Added clear Reminders and AI Coach placeholder pages. Reminders previews supplement/medication completion and BP prompts without scheduling notifications yet; AI Coach documents the future consented, wellness-only direction without connecting any AI service.
- Verified lint, strict TypeScript, 21/21 tests, `git diff --check`, and a successful iOS bundle export.

## 2026-08-31 — Cross-agent context handoff audit

- Added a root `CLAUDE.md` that loads the authoritative working agreement, current status, and roadmap; the mobile Claude file now inherits that context as well as its Expo-specific rule.
- Updated the working agreement for the current iPhone/EAS, hosted Supabase Edge Function, browser-preview, user-learned suggestion, and dirty-worktree constraints.
- Replaced the stale Releases 0–1/Expo Go README with the current HealthHub, dev-client, HealthKit, camera, and barcode workflow.
- Recorded the uncommitted nutrition/barcode feature set, already-deployed backend state, pending camera-enabled EAS binary, and current `Track` navigation label in the concise handoff.

## 2026-08-31 — Barcode nutrition and household-serving correction

- Reproduced the missing-nutrient problem against a real PopCorners UPC: Open Food Facts v3 returned the requested identity/serving fields but not the legacy `nutriments` object expected by the resolver, while its v2 endpoint returned the normalized nutrient fields.
- Moved product lookup to the documented v2 product endpoint, added kilojoule calorie fallback and US total-carbohydrate preference, sentence-cased all-uppercase provider food names, and versioned the server cache so previously broken rows refresh on their next scan.
- Removed the unsafe fallback that treated a named but unweighted package as 100 g. The provider's 100 g basis is used only when it supplies no serving description at all.
- Added generic package/piece/bar-style household conversions across catalog products, private profiles, meal drafts, immutable history snapshots, manual label editing, and amount calculations. Partial piece counts scale through the provider's serving quantity without guessing mass or density.
- Applied `202608310002_food_household_servings.sql` to HealthHub and deployed ACTIVE `resolve-food-barcode` version 3, including protection against serving a pre-fix stale cache row when the provider is unavailable. Remote schema lint reports no errors.
- Verified the full check suite (27/27 mobile tests and 5/5 provider-normalization tests), `git diff --check`, and a production web export. Live iPhone rescanning of the corrected PopCorners flow remains the active validation item.

## 2026-09-01 — Detailed package nutrition and liquid-volume correction

- Reproduced both phone findings against the live Open Food Facts records. PopCorners `0810607023346` exposes a generic 100 g serving in the flat v2 product but a distinct 240-calorie packaging serving in its structured detailed panel; Fairlife `0811620021968` imports `1 bottle (414 g)` despite an explicit 14 oz liquid package.
- Added a guarded v3 knowledge-panel lookup only for generic 100 g records whose flat serving values duplicate the per-100 g values. The resolver parses the packaging-serving column, infers the 49.6 g package basis from its calorie ratio, and returns the detailed package nutrients rather than the flattened 100 g values.
- Added a constrained liquid-container rule: an explicit package ounce quantity is treated as fluid volume only when OFF also identifies a one-container liquid product. Fairlife now returns one `14 fl oz` bottle, 414 mL, and no weight conversion; no density conversion is introduced.
- Suppressed zero-valued count/weight/volume conversions while retaining valid zero nutrient values, and clarified in the label editor that most products should use weight or volume according to the package, leaving the other blank unless both are explicitly stated.
- Applied `202609010001_food_provider_servings.sql`, which expires pre-version-3 barcode cache rows, and deployed ACTIVE `resolve-food-barcode` version 4 with JWT verification. Live normalization returns PopCorners at 49.6 g/240 calories and Fairlife at 414 mL/170 calories.
- Verified `npm run check` with 27/27 mobile tests and 7/7 provider-normalization tests, plus `git diff --check`. Registered-iPhone re-scanning and Food History confirmation remain the active validation step.

## 2026-09-01 — Open Food Facts v3.6 contract and lookup-error correction

- Audited the implementation against the official Open Food Facts API introduction and v3 OpenAPI source. The documentation now marks v2 deprecated and recommends v3; v3.6 exposes structured nutrition through `nutrition.aggregated_set` and `nutrition.input_sets` and reports lookup outcome through `result.id`.
- Replaced the v2 product request plus generated knowledge-panel parsing with one identified `GET /api/v3.6/product/{barcode}.json` request using an explicit field list. The resolver accepts `product_found`, negative-caches `product_not_found`, and treats malformed responses, non-success HTTP statuses, timeouts, and rate limits as provider failures.
- Removed the inferred PopCorners package size. Its live OFF record does not state that one serving is one package and its two packaging input sets are internally inconsistent, so the app now retains the explicit 240-calorie per-serving nutrients while leaving serving description/count/weight/volume blank for confirmation from the bag.
- Added generic structured-unit handling: a `100ml` provider basis produces volume, a `100g` basis produces mass, and a reliable serving input uses its own unit. Explicit one-container fluid quantities remain volume-only without checking a product name, brand, or barcode.
- Added typed mobile lookup errors. Confirmed not-found products open the private label editor automatically; invalid UPC/EAN values remain on the scanner with digit guidance; provider/network failures remain on the scanner with retry and manual-barcode guidance.
- Applied `202609010002_open_food_facts_v3.sql`, deployed ACTIVE `resolve-food-barcode` version 6 with JWT verification, and confirmed the linked migration list and schema lint. A generic conflict guard now also leaves conversions blank when OFF reports a 100 g aggregate for a package explicitly labeled by volume. Verification passes with 27/27 mobile tests and 10/10 Edge normalization/contract tests; live v3.6 checks cover inconsistent serving data, a 414 mL bottle, and a separate 355 mL soda serving.
- Removed the EAN-13/EAN-8/UPC-A/UPC-E selector from manual entry. Camera scans continue to use Expo's detected symbology, while manual values are detected from their digits and check digit. OFF now receives the original UPC/EAN digits for its documented leading-zero normalization; UPC-E is expanded only to validate its check digit. Deployed ACTIVE resolver version 8 and verified the full suite with 27/27 mobile tests and 14/14 Edge tests.

## 2026-09-01 — Unified food lookup and reusable-profile deduplication

- Removed Basic entry as a separate new-food path and combined it with saved-food search under **Find or add food**. Recent and reusable matches continue to open the amount editor; a non-empty name without an exact normalized match now asks whether to create a reusable label and prefills that name. Existing unfinished/basic entries remain editable through the common amount model.
- Canonicalized reusable-profile barcodes and made `saveFoodProfile` reuse an existing per-user profile by either catalog product ID or barcode. Name-only profiles are deliberately not merged because the same name can represent different brands, servings, and nutrition.
- Added and applied `202609010003_user_food_profile_barcode_dedup.sql`. It normalizes existing profile barcodes, repoints immutable nutrition-history references to a selected keeper, deletes only the redundant profile rows, and enforces `unique (user_id, barcode)`. The linked migration list is current and remote schema lint reports no errors.
- Verified lint, strict TypeScript, 29/29 mobile tests, 14/14 Edge tests, and `git diff --check`. The TypeScript UI change requires only a Metro/app restart, not another EAS build.

## 2026-09-04 â€” Local iPhone reminders

- Replaced the Reminders placeholder with user-scoped on-device routines for named medications/supplements, Custom, Blood pressure, and Weight tracking.
- Added native date/time selection and one-time, daily, selected-weekday, and weekly schedules. Saving an active routine requests iOS notification permission and schedules a local system notification that shows while the app is foregrounded or closed; tapping it returns to Reminders.
- Added per-local-day completion toggles, routine editing/deletion with cancellation of old schedules, and the requested History-tab order: Workout, Food, Weight, Blood pressure.
- Installed Expo SDK 57-compatible `expo-notifications` and `@react-native-community/datetimepicker`, and configured their native app plugins. These local schedules/completion marks intentionally remain on the signed-in device for now; no health/reminder data is sent to a new backend.
- Replaced the third-party UUID call in reminder creation with the app's React Native-safe UUID helper, which falls back safely when `globalThis.crypto` is unavailable on a device.
- Added a Multiple daily repeat option with up to six distinct daily notification times, and removed the Meal and Water reminder choices in favor of a named Custom choice. Workout now suggests the current user's recent saved gym locations while filling its location field.
- Suppressed the redundant lock-screen error when an automatic Face ID request is already in progress; real Face ID errors still remain visible.
- Added HealthKit heart-rate permission plus read-only pulse import. Because HealthKit does not include pulse in the blood-pressure correlation, the import and History UI use the closest value within two minutes while preferring an identical source label; this accommodates device/source-label differences while the pulse remains non-editable and is not deleted with the BP reading. The Apple Health card resumes its original connected-state display after authorization.
- Existing HealthKit connections now do one deterministic, deduplicated one-year BP re-read on the next sync to backfill matching pulse data rather than waiting for a newly-recorded BP measurement. Verified lint, strict TypeScript, 82/82 mobile tests, production web export, and `git diff --check`.
- Replaced deprecated DateTimePicker `onChange` handlers with `onValueChange`, and migrated the remaining React Native SafeAreaViews (the Face ID gate and Food editor) to `react-native-safe-area-context`, removing both device warnings without changing their layout.
- Verified lint and strict TypeScript; `npm run test` passes 77/77 tests. The initial sandboxed test attempt was blocked by Windows `spawn EPERM`, then passed unchanged with approved local worker-process access.

## 2026-09-04 ? iOS-inspired interface refresh

- Reviewed navigation, Summary card density, History filtering, Quick Log, Profile, authentication, tracking forms, safe areas, and existing chart/photo gestures. The main problems were inconsistent styling, missing tab icons, cramped nutrition card content, static controls, and inconsistent scrolling/keyboard treatment.
- Added reusable theme, line icons, spring press feedback, segmented controls, safe-area-aware page scrolling, reduced-motion context, and motion-aware modal wrappers under `apps/mobile/src/ui`. Existing business logic, privacy boundaries, imported-data restrictions, persistence, and remote services are preserved.
- Refreshed the app shell and major screens with grouped iOS-style surfaces, white cards, system blue actions, category accents, larger headings, and less heavy typography. Quick Log has six icon tiles in a scrollable, expandable native sheet. Summary separates latest readings from daily nutrition and gives rings enough room. Profile and authentication have clearer visual identity, and sign-in handles smaller screens and the keyboard.
- Added horizontal History category gestures with distance/velocity thresholds, diagonal/vertical rejection, bounded navigation, optional buttons, scroll reset, and cancellation cleanup. Kept the existing continuous vital-chart gestures and photo paging.
- New motion respects native Reduce Motion and CSS prefers-reduced-motion; all modal wrappers honor the same preference. No package or native configuration changes were required.
- Read the repository-required Expo SDK 57 documentation at https://docs.expo.dev/versions/v57.0.0/ before implementation.
- Validation: lint and strict TypeScript pass; 87 mobile tests and 16 Edge tests pass; production web export and `git diff --check` pass. Tests initially failed to spawn workers in the sandbox, then passed unchanged with approved process access.
- Browser visual QA could not run: the Browser skill/runtime connected but reported no available browsers. Native layout, keyboard, VoiceOver, sheet dismissal, and gesture feel must be validated on the registered iPhone, especially at increased text size and with Reduce Motion enabled.

## 2026-09-08 — App review and end-to-end usability fixes

- Preserved the pre-existing uncommitted interface refresh. Read the SDK 57 reference and tested the real Expo app in isolated headless Edge after the Browser runtime reported no connected browsers.
- Reduced Quick Log height with compact icon rows; streamlined Summary, History, meal and Coach copy; fixed 320 px greeting/wrapping; added food-method and camera/library icons, selected hydration shortcuts and accessible form/control states.
- Fixed direct Quick Log sign-in protection, reset/sign-in validation, browser SVG and populated-chart event warnings, missing manual pulse in BP History, disappearing workout-save feedback and empty workout cleanup after failed set insertion. Added individual fluid rows and confirmation-based deletion in Food History.
- Browser Reminders now states that scheduling requires the iPhone instead of showing a form with no working time picker. Chart points support browser keyboard inspection without changing the native press handler.
- Added `npm run test:e2e`, a Playwright configuration that starts its own server, and a synthetic Supabase fixture. All 8 browser tests pass together across the main flows and four viewport widths. No real health data, credentials, emails or remote database changes were used.
- Verification: lint, strict TypeScript, 90 app tests, 16 Edge tests, production web export, iPhone/Hermes bundle export and whitespace checks pass. The sandbox initially blocked test-worker spawning; approved process access allowed the checks to run normally.
- Detailed changes, commands, scope and outstanding native/live-service checks are in `docs/APP_REVIEW.md`. Face ID, camera/photos, notifications, Apple Health, native accessibility/gestures and live Supabase/RLS still require a disposable account and the registered iPhone's current development build. No deployment or commit was made.
- Completion audit confirmed no connected Apple mobile device or available iOS controller and no test-account environment variables. Corrected stale status entries for the old Quick Log grid, browser verification and test totals. The full goal remains open pending live-service and native-device evidence.
- After the same access blocker persisted across three goal turns, marked the goal blocked pending external access or hands-on native results. An approved read-only Windows device query confirmed zero connected Apple mobile devices; the current Playwright result still reports passed with no failed tests. No additional app changes were needed during this audit.

## 2026-09-09 — Correct Expo workspace startup

- Investigated the reported iOS `Unable to resolve "../../App"` failure. The running port-8081 manifest identified the repository root as the Expo project; `apps/mobile/package.json` already selected `expo-router/entry`. The README and barcode guide incorrectly recommended bare `npx expo start` without selecting the mobile directory.
- Added root `start` and `web` workspace aliases and explicit argument forwarding to root `dev`, `start` and `web`. Updated both guides with the repository-root command and instructions to stop/restart an incorrectly launched server.
- Read the required Expo SDK 57 reference before editing. Verified `npm run dev -- --dev-client --offline --port 8083 --max-workers 2` selected `apps/mobile`; requested its iOS manifest and actual development bundle, which returned HTTP 200 after Metro bundled 1,750 modules. Offline mode only avoids Expo CLI network calls during this verification. Stopped the temporary server; the user's existing port-8081 process requires a restart with the corrected command.
- Validation: `npm run check` passed (lint, strict TypeScript, 90 app tests and 16 Edge tests); whitespace checks passed. The initially sandboxed Metro process could not spawn DevTools or reach Expo services, so the successful bundle verification used approved worker access and Expo's offline CLI mode.

## 2026-09-09 — Match fluids to Food History meal formatting

- Compared the previous daily Food History layout with the review's added fluid rows. Moved drinks below meals into a Fluids group within the existing daily card, using the same section title, name/time header, amount text, row dividers and compact bordered action as food entries. Removed the separate icon-led fluid row presentation; the original daily fluid total stays in the date header.
- Extended the existing water flow with a same-day meal so it checks cancel/confirm deletion, fluid-total recalculation and meal preservation. Both targeted water/meal browser flows passed. Visually reviewed synthetic-data screenshots at 320 and 390 px; `npm run check` passed lint, TypeScript, 90 app tests and 16 Edge tests. Native rendering was not exercised for this layout-only change.

## 2026-09-09 — Preserve the unlock destination and unify tracking screens

- Found that the biometric lock branch unmounted the entire tab navigator, recreating it at Summary after unlock. Added a privacy boundary that leaves navigation and draft state mounted, conceals content, disables interaction/accessibility and dismisses the keyboard. Shared native dialogs observe the same privacy state. Removed Quick Log's lock-time Summary redirect so root sheet dismissal reveals the prior tab.
- Added two React lifecycle tests using native host stand-ins: selected screen and unsaved input survive repeated locks, and open private dialogs hide immediately until unlock. Added matching React renderer/type and esbuild development dependencies for these component tests; no native runtime dependencies or configuration changed.
- Shared tracking typography, inputs, cards, selection chips and primary/secondary action styles across Food, Water, Weight, Blood pressure, Workout/Cardio, Reminders and the food editor. Removed the extra Cardio heading and Hide/expand control. Replaced duplicate lifting Add controls with one Add exercise action below the entries; moved Add food below its list. Removed numbered section labels and redundant empty-list copy, aligned food totals with grouped cards, corrected browser input fonts and narrow water-shortcut wrapping.
- Verification: all 8 browser flows pass, including Cardio screenshots at 320/390/430/1280 px and populated draft screenshots; lint, strict TypeScript, 92 app/component tests and 16 Edge tests pass. iOS/Hermes export bundled 1,695 modules successfully. Native Face ID interaction, foreground transition timing and VoiceOver still require the physical iPhone. Changes remain local; no deployment or commit.

## 2026-09-10 — Smooth exercise reordering and clearer Add buttons

- Replaced fixed-distance exercise swaps with a gesture-driven draggable list: cards follow the finger, adjacent cards slide aside, a spring settles the drop, and the list scrolls near its edges. Variable card heights and current reps/weights survive reordering; keyboard and accessibility move actions remain available. Reduce Motion suppresses the lift/settling animation.
- Made Add food and Add exercise full-width, high-contrast blue buttons with white plus icons and labels. Kept them below their lists. Removed the requested saved-exercise/history hint and corrected cramped numeric-field padding.
- Added the JavaScript-only draggable-list dependency using the existing Gesture Handler/Reanimated modules, plus a pinned, reproducible patch for browser scroll measurements and competing scroll gestures. Root E2E scripts now forward test filters.
- Verification: all 9 browser flows pass together, including pointer tracking, repeated/variable-height reordering, edge auto-scroll, keyboard movement, draft restoration and exact saved sets. Lint, TypeScript, 92 app tests, 16 Edge tests, patch application and iOS/Hermes export pass. Reviewed synthetic food/workout screenshots. Physical iPhone gesture feel, VoiceOver and Reduce Motion remain hands-on checks.

## 2026-09-11 ? AI Coach meal labels from photos and descriptions

- Reviewed project/agent docs, roadmap, architecture and recent commits. The tree was clean at d5a8acb; the existing nutrition/profile/draft workflow supports the new feature without replacing prior tracking behavior. The previous AI Coach page was a placeholder.
- Added camera/library/text meal input with per-request OpenAI consent, separate component estimates, editable food names/descriptions/portions/all seven nutrients, confidence/assumptions, removal, revision, cancellation and error recovery. Labels use a distinct 100 g/100 mL nutrient basis and consumed quantity. Acceptance creates/reuses private labels and appends to the existing user-scoped draft; Save meal retains immutable snapshots.
- Added a shared Zod contract, OpenAI Responses integration, server authentication, bounded JPEG/text bodies, strict output validation, no raw-input logging, timeouts and an atomic 20-attempt per-user UTC-day quota. API keys remain server-side; response storage is disabled without claiming zero provider retention. Declared the shared Zod version at the workspace root; no native dependency was introduced.
- Added label descriptions and AI provenance to the existing food model/schema and preserved them through profile management, reuse and History. Applied migration 202609110001_ai_meal_estimates.sql to canonical HealthHub and deployed ACTIVE estimate-meal v1 with JWT verification using the hosted API (no Docker). Database lint is clean; unauthenticated endpoint returns 401.
- Hosted secret inventory has no OPENAI_API_KEY, and app environment files contain only Supabase public configuration. No real LLM call was made. Completed the user-authorized fallback: deployable/running surrounding feature, explicit not-configured behavior, server secret example, AI_MEAL_SETUP.md and ADR-004. Key activation and real food-photo evaluation remain follow-up work.
- Verification: npm run check passes lint, strict TypeScript, 96 app tests and 22 Edge tests; Deno checks the actual Edge entry point; iPhone/Hermes export passes. Nine existing browser flows plus four new AI flows pass using synthetic accounts/responses/images. The added cancellation test initially read the fixture before the asynchronous profile write completed; it now waits for the visible saved portion, and all four AI tests pass together. Reviewed 320 px Coach and 320/1280 px estimate screenshots. In-app Browser reports no connected browsers, so verification used the repository's isolated Edge harness.
- Native camera permission/orientation, keyboard/VoiceOver and Face ID transitions, live authenticated LLM success/quality, quotas and cross-user RLS remain explicitly unverified. Existing native-validation backlog is preserved. No app code commit or EAS binary deployment was made.

## 2026-09-11 — Correct AI secrets setup path

- Diagnosed the secrets-file error: the command was run in apps/mobile, while the guide assumed the repository root; the local secrets file was also absent. Created the Git-ignored .env.local from its placeholder example without overwriting an existing file. Updated setup/status with explicit directory, file creation and private key-entry steps. Verified the file exists and is ignored by Git. No secret was supplied or uploaded; no application code changed.

## 2026-09-11 — Diagnose unfunded API access and reduce meal-estimate cost

- Confirmed the configured OpenAI key can access model metadata, while the user confirmed the API project has no funded balance. OpenAI's model documentation lists free-tier access as unsupported, so billing is the expected blocker for live meal estimates.
- Changed the deployed default from `gpt-5.6-terra` to vision-capable `gpt-5.4-mini`, reducing documented input and output token rates by 62.5%. Retained high-detail image analysis and strict structured output while lowering the output cap from 6,000 to 3,500 tokens.
- Added safe provider-error classification so the app identifies exhausted credits/spend limits, authentication, access, model, rate-limit, request and availability failures without exposing meal content, provider bodies or credentials. Updated the hosted model secret and deployed the revised function.
- Verification passes with 24/24 Edge tests, Deno checking the deployed entry point and whitespace checks. Live meal quality remains blocked until OpenAI API billing or prepaid credits are added.

## 2026-09-11 — Add AI serving/count estimates, daily limit visibility and duplicate suppression

- Replaced the AI-only 100 g/100 mL result contract with the nutrition model's one-serving basis. The model now fills a short serving label, estimated weight/volume and, for countable foods, a singular item unit and items per serving. `8 pork dumplings` becomes one label with nutrition for `1 dumpling`, an editable portion of `8 dumplings`, and an estimated gram conversion.
- Added serving, item, gram and milliliter choices to AI review when their conversions are available. Names, short descriptions, serving labels, item units, counts, conversions, portions and nutrients remain editable; saved meal notes now contain only AI confidence.
- Kept the database-atomic limit at 20 requests per authenticated user per UTC day and exposed the exact limit in the UI and rejection message. The photo and description are explicitly treated as the same meal; normalized repeated model foods and identical draft entries are suppressed without summing the same meal twice.
- Normalized generated and edited AI food names so the first letter is capitalized both in review and at the final save boundary. Replaced the understated Food action with a prominent purple `Estimate meal from photo or text` card and an explicit `AI creates editable food labels and portions` subtitle; AI Coach uses matching input-focused wording.
- Matched the estimator to the existing food editor's safe-area-aware `pageSheet` and fixed bordered header so the title and close action cannot load past the top edge. Extended duplicate matching to ignore common preparation words and consolidate contained names with the same food head, such as `dumplings` and `steamed pork dumplings`, while retaining a distinct food such as `pork dumpling sauce`.
- Fixed the persistence-level duplicate path: repeated estimates now match active AI profiles by normalized food identity despite changing descriptions or nutrition, preserve a user-corrected profile, archive older matches, hide existing duplicate AI profiles from suggestions, suppress the same AI identity/profile within a draft, and collapse older duplicate AI draft entries when a draft loads. The prompt now reserves preparation wording for descriptions so canonical label names remain stable.
- Verified lint, strict TypeScript, 98/98 app tests, 26/26 Edge tests, Deno type-check and all 5 AI browser flows. The repeated-estimate flow seeds an older duplicate, returns the same food under a preparation variant, and confirms one corrected active profile remains and the older match is archived. Deployed authenticated `estimate-meal` v11 ACTIVE and confirmed the quota migration remains linked remotely.

## 2026-09-11 — Reject non-food meal photos and reorder Food entry

- Added strict `meal`, `not_food`, and `unclear` AI output classifications. Non-food or unclear results cannot produce labels even if the provider returns contradictory items; the editor keeps the selected photo and description visible and gives a concise retry message.
- Kept meal selection at the top, removed the choose-a-meal empty box, allowed Add food before meal selection, and changed its icon from plus to food. Save meal becomes available once food exists and displays `Choose a meal first.` until breakfast, lunch, dinner, or snack is selected.
- Verified lint, strict TypeScript, 98/98 app tests, 27/27 Edge tests, Deno type-check, all 6 AI browser flows and all 9 existing browser flows. Deployed authenticated `estimate-meal` v13 ACTIVE with JWT verification.

## 2026-09-11 - Implement private AI health, fitness, and bodybuilding Coach

- Replaced the Coach placeholder with explicit setup/consent, category controls, saved threads, quick prompts, chat, dated evidence, explicit citations, quota/retry/cancel feedback, settings, deletion, and a compact link to the separate meal-photo estimator.
- Added editable next-meal and next-workout action review. Meal suggestions prioritize foods explicitly provided by the user, can create short estimated private labels only after approval, and otherwise use owned saved-food labels. Nutrition is recalculated at application time. Training suggestions use recent frequency and latest-versus-prior progression and support lifting, cardio-only, combined lifting/cardio, added abs, and rest. Approved lifting/cardio plans become recoverable local drafts; rest records nothing. Existing drafts require Cancel, Append, or Replace and never become completed logs automatically.
- Added private Coach tables with RLS, same-owner cascading foreign keys, atomic service-only quotas, a compact historical snapshot, category-gated bounded read tools, rolling context, strict structured output, three-round tool limit, safety action suppression, prompt-injection boundaries, and content-free provider diagnostics. OpenAI Responses use `store:false`, conservative input budgets, 1,400/2,400 output caps, no paid retry, and request cancellation propagation.
- Applied `202609110002_ai_coach.sql`; linked database lint is clean. Deployed authenticated, timezone-aware `coach-chat` v5 ACTIVE with content-free operational telemetry, a fail-closed rollout guard, and configured Luna/Terra model secrets. The hosted `AI_COACH_ENABLED=false` flag remains off because the API project is unfunded; live grounding, cost, latency, citations, and safety evaluation are still required before activation.
- Verification: lint, strict TypeScript, 101 app tests and 38 Edge tests pass. Deno checks the deployed entry point. All 21 browser flows pass, including 6 Coach flows for setup, saved/new meal labels, workout conflicts, cardio routing, thread restoration/deletion, plus all prior tracker/AI meal regressions. Documentation is in `AI_COACH_SETUP.md` and ADR-005.

## 2026-09-11 - Enable Coach, multi-goal setup, and timestamp compatibility

- Replaced the single primary-goal control with a multi-select goals list. Added and remotely applied `202609110003_ai_coach_multi_goals.sql`, which backfills every existing Coach profile from `primary_goal` and retains that column for older app compatibility.
- Fixed `Invalid ISO datetime` after loading real Coach profiles by allowing valid PostgreSQL timestamps with explicit UTC offsets. The Edge and browser fixtures now use the exact `+00:00` form that previously failed before a suggested prompt could run.
- Deployed JWT-protected `coach-chat` and set the hosted `AI_COACH_ENABLED=true` secret at the user's request.
- Diagnosed the first live failure with a synthetic request through the exact production handler. OpenAI rejected Zod's generated `uri` format and nested `oneOf` keywords. The handler now removes unsupported format annotations and maps nested `oneOf` to supported `anyOf`, while still parsing every returned response and action through the original strict Zod contracts.
- Verification: lint, strict TypeScript, 104 app tests, 42 Edge tests, Deno entry-point check, and all 7 Coach browser flows pass. The Coach home now has a compact, uncluttered layout with conversation history hidden until requested and is visually covered at a 320 px phone width. The workout flow applies a Coach plan, removes an intentionally incomplete prior exercise, finishes the generated workout, and verifies all generated sets were saved. Production-shaped meal, workout, and progress-review requests receive OpenAI HTTP 200 and valid Coach responses. The remote migration list includes `202609110005`; `coach-chat` v11 is ACTIVE with JWT verification. Failure telemetry and client errors separate provider capacity, model formatting, action validation, and conversation persistence. After authenticated testing isolated conversation persistence, `save_coach_turn` replaced three separate writes with one owner-checked transaction so partial turns roll back. Failed requests now refund quota reservations, today's rollout counters were reconciled to saved assistant turns, and malformed optional model items no longer discard an otherwise valid answer.

## 2026-09-15 - Preserve password unlock route and improve workout/food entry

- Carried the current authenticated pathname into the password sign-in fallback and validated the destination before restoring it. Both password submission and the Face ID option on that sign-in screen use the saved route. Added route-preservation coverage alongside the existing privacy-boundary tests.
- Added explicit unilateral-name recognition and draft validation. The lifting form shows left/right rep and weight rows, stores right-side values on the same logical workout set, and keeps them intact in history editing. Added a reviewed migration and replaced the workout-edit RPC to accept the additional fields.
- Disabled held-card edge autoscroll so page headers, location, notes, and actions do not race away while reordering. Increased the hold delay, softened the lift spring, and added visible one-step arrow controls for predictable positioning.
- Reflowed serving measurement inputs and unit chips across the available width. Added optional total servings per product to manual/saved/provider labels and the Open Food Facts cache. Provider serving-size edits scale calories and nutrients from their original basis by default; users can turn scaling off and edit incorrect nutrition independently. Removed the provider source notice.
- Applied both new migrations to the linked HealthHub project after a clean dry run, then deployed `resolve-food-barcode` normalization v5. Workspace lint and strict TypeScript pass; 115 app tests, 43 Edge tests, and the production iOS/Hermes export pass. Physical iPhone checks remain for password navigation, unilateral input ergonomics, drag feel, narrow food layout, and keyboard behavior.
- Follow-up: increased the serving-scaling control contrast with a bordered surface, solid gray/blue track, white thumb, and visible ON/OFF state. Weight and volume label inputs now use an iPhone keyboard containing `/` and accept strict decimals, simple fractions (`1/3`, `3/4`, `1/2`), and mixed fractions (`1 1/2`). Invalid and zero-denominator fractions remain blocked. Lint, strict TypeScript, and all 116 mobile tests pass.
- Follow-up: when an existing label supplies both weight and volume, changing either measurement with scaling enabled now applies the same ratio to the paired measurement and nutrients. This preserves the product's explicit weight-to-volume relationship without guessing density when only one measurement exists. The toggle copy now describes serving details and nutrition, and turning it off keeps every field independently editable.
- Restored the food-editor UI after that file was overwritten with its earlier implementation while the model and database changes remained. Paired scaling now also establishes its baseline after both measurements are filled or whenever scaling is switched on. Added direct ratio coverage; lint, strict TypeScript, and all 117 mobile tests pass.

## 2026-09-16 - App-wide iOS visual redesign

- Audited every route and shared visual module. Established a restrained light palette, readable type hierarchy, 16-point content surfaces, 10-point form controls, semantic feedback, and shared motion durations. Consolidated fields, actions, editor toolbars, dialogs, and section headings across the existing Expo Router architecture.
- Refined Summary readings/units/categories and grouped nutrition; replaced SVG progress numerals with native text and a large-text layout. Flattened the center tab, turned Quick Log into one grouped list, and applied SF Symbols on iOS with vector fallbacks. Updated auth, History/detail editors, Food/AI/label sheets, water/vitals/photos, lifting/cardio, reminders, Coach/setup/review, and Profile.
- Preserved data/integrations and existing drag behavior. Added keyboard adjustment to the food editor, keyboard avoidance to transparent dialogs, shared input focus/disabled feedback, and reduced-motion-aware press timing. Fixed narrow signup field overflow and tab-label clipping found in rendered QA.
- Verification: lint and strict TypeScript pass; 117 app tests and 43 Edge tests pass; all 23 isolated browser flows pass, with an additional targeted compact-form pass after the signup sizing correction. The production iOS/Hermes bundle exports successfully. Reviewed synthetic screenshots at 320 x 568, 390 x 844, 430 x 932, and desktop widths, plus populated charts, Coach, authentication, and modal states. Updated the privacy test native mock for keyboard avoidance/platform selection and aligned stale drag assertions with the already-existing activation threshold and disabled autoscroll.
- No backend changes, deployment, or user-data writes. Native keyboard/VoiceOver/Dynamic Type, SF Symbols, sheet gestures, Face ID, camera/photos, notifications, and HealthKit still need the registered iPhone. Light-only appearance remains as configured. Design notes and QA scope are in docs/DESIGN_SYSTEM.md.

## 2026-09-16 - Preferred UI restoration and targeted interaction revisions

- Restored the pre-redesign global theme, navigation, Summary, trackers, sheet and press styles at the user's request. Preserved Profile using isolated profile-theme tokens and its native input wrapper. Reworked login with a compact brand, visible field labels and clear authentication hierarchy; Coach has a quieter toolbar, stacked suggestions, composer and grouped consent/setup sections.
- Charts now acquire every touch at its start, including diagonal re-grips, and release parent scrolling on completion/cancellation. Native tap selection uses measured SVG coordinates so inspection survives responder capture; web keyboard/click inspection remains available.
- Removed drag controls, the draggable FlatList dependency, its compatibility patch and unused drop helpers/tests. A normal FlatList and 44-point up/down icon actions retain exact order and drafts. Unilateral exercises use just L/R, two aligned rows, shared column widths and horizontally scrollable reps; removed the Side heading and detection message. Both-side values continue to persist in one logical set and are editable in history.
- Added a reduced-motion-aware animated water glass driven by saved totals and the Profile hydration goal, with goal completion and unset-goal states. Protein now has a distinct food outline. Edit/delete/remove actions use labeled 44-point icons, preserving existing confirmations and callbacks.
- Verified lint, TypeScript, 115 app tests, 43 Edge tests, all 26 browser flows, clean postinstall with no active patches, and iOS/Hermes production export. Browser coverage includes vertical-leading diagonal repeated touches on both graphs, cancellation/page-scroll recovery, arrow ordering and persistence, 320px L/R alignment and both-side saved values, water failed-save/retry plus changed/unset goals, and icon edit/delete paths. Updated obsolete visible-text test selectors for icon actions. Inspected synthetic login, Coach, water and workout screenshots; broader screen captures cover 320/390/430/1280px.
- Browser plugin discovery returned no connected browsers, so repository Playwright fixtures provided isolated visual and flow verification. Physical-iPhone gesture feel and reading taps, Dynamic Type, VoiceOver, keyboards and existing Face ID/HealthKit/photo/notification integrations remain device checks. No backend deployment or user records were changed.

## 2026-09-16 - L/R alignment correction

- User reported remaining L/R misalignment. Each side previously had an independent horizontal ScrollView, allowing input focus or scrolling to shift only one row; the original two-set browser assertion did not exercise that condition.
- Added shared ExerciseSetFields for workout entry and saved-workout editing. Each set is a single column containing its L/R inputs inside one horizontal scroll viewport. All side labels, rep inputs, weights and units use the same explicit row height, including font scaling. Removed duplicated row/spacer styles.
- Extended the browser regression to overflow with six sets, alternate focus across sides, assert matching columns and field heights, and edit/save right-side history reps without changing the left side. Focused lifting, arrow ordering and save flows pass; the final extended alignment test passes. Reviewed the synthetic 320px screenshot. Lint, TypeScript, 115 app tests and 43 Edge tests pass; device visual confirmation remains outstanding.

## 2026-09-16 - Weight photo isolation, daily count and balanced confirmations

- Traced photo leakage to an unfiltered global gallery query: the weight ID selected a starting index but did not restrict the records. Made the weight ID required for gallery reads and filter it before paging/signing; History mounts a separate gallery per selected weight. Uploads continue using that exact weight ID. No metadata migration or guessed reassignment is needed.
- Replaced the loaded-gallery-derived daily counter with an independent user/local-day metadata query. It refreshes after upload and deletion, counts photos from all weight entries, and distinguishes loading/unavailable from zero. Limited the viewer height and made details scrollable so the daily limit remains reachable and fully visible at short phone heights.
- Added shared ConfirmationActions: equal-width, minimum-48-point Cancel/Delete text buttons with the same typography, padding and corner radius. Applied to History, progress photos, saved food-label deletion, Coach conversation deletion, and meal discard; preserved icon entry points and existing handlers.
- Added isolated browser coverage for three weight entries, scoped signed-URL requests, an empty entry, a real synthetic library upload associated to that entry, daily quota controls, cancel/confirm, deleting an older photo without reducing today's count, deleting today's photo with count refresh, and equal confirmation dimensions. Reviewed synthetic screenshots. Four focused photo/Coach/meal/fluid flows pass, plus the final short-screen photo run. Lint, TypeScript, 115 app tests and 43 Edge tests pass. Native camera/gallery confirmation remains a device check. No backend deployments or user data writes.

## 2026-09-16 - Per-entry photo capacity and centered controls

- User requested three photos per entry instead of per day. Replaced the daily counter and upload guard with entry-scoped queries and explicit per-entry copy. Removed the obsolete daily-slot allocator/test. Uploads now require a weight ID.
- Applied migration 202609160001 to the linked hosted project. It drops daily uniqueness, preserves existing rows/objects and legacy metadata, and enforces the three-photo count in a trigger with a per-entry transaction lock. Existing over-limit groups remain readable/deletable and reject new uploads until below three.
- Centered the two equal-sized Camera/Library buttons and moved the trash entry point beside photo details. Existing balanced text confirmation actions remain.
- Lint, strict TypeScript, 114 app tests, 43 Edge tests and the revised focused browser flow pass. Browser coverage proves independent capacity with more than three uploads across the same day, accurate old/new attachment counts, delete/reuse, cancellation and centered controls at 320px. Waited for modal animation before measuring geometry; reviewed the resulting screenshot. Remote temporary-table SQL checks pass, database lint finds no errors, and dry-run confirms migrations are current. No existing user photo records or files changed; native picker/device layout confirmation remains open.

## 2026-09-16 - Login refinements and visible Summary sync

- Centered Face ID cancellation/error text on the privacy lock. Moved the login brand to top center, removed the welcome/subtitle and conditional Face ID-removal hint, added accessible eye/eye-off password visibility controls, and replaced Remember me checkbox with the native Switch while retaining persistence handlers. Shared sign-up password entry also supports visibility.
- Existing pull-to-refresh already ran the combined sync/load. Set a safe-area offset, tint, refresh title and alwaysBounceVertical so its native indicator is visible; Sync now displays an activity indicator until loading finishes. Audited all sync call sites: Summary and History focus/refresh plus vital mutations, with Apple Health imports on Summary if connected. No timed or background HealthKit sync.
- Lint, strict TypeScript, 114 app tests and 43 Edge tests pass; the compact forms browser regression passes. Added focused password-value retention, toggle persistence/clearing across logout and sync busy/failure/retry browser checks. Native Face ID, password keyboard/autofill and pull-to-refresh placement remain device checks. No backend changes.
- Final focused browser checks pass (two new flows plus the earlier compact-form flow); reviewed the 320px login screenshot. The tests use the DOM text-input property and a non-retrying synthetic error to avoid mistaking default HTML attributes or backend retry delays for UI failures.

## 2026-09-16 - Compact login options row

- Moved the Remember switch/label to the left and the enrolled Face ID action to the right of one row below Password. Sign in remains immediately beneath it. Removed the older Face ID card, account subtitle and OR divider; kept accessible account labeling and authentication/busy behavior.
- Lint, TypeScript, 114 app tests, 43 Edge tests and the existing login password/Remember browser flow pass. Reviewed the 320px web screenshot; enrolled native Face ID presentation remains an iPhone check.

## 2026-09-16 - Remember toggle native alignment

- The parent row was centered, but React Native on iOS supplies alignSelf flex-start on Switch. This placed its 31-point control at the top of the taller row while the label remained centered. Added an explicit alignSelf center to override that native default. No toggle behavior changed. Native iPhone visual confirmation remains open.
- Verification: lint, strict TypeScript, 114 app tests and 43 Edge tests pass. Log: dist/remember-alignment-check.log. The fix targets the confirmed iOS-specific default; no redundant web layout test was added.

## 2026-09-16 - Login secondary-link spacing

- Increased the space below Sign in before Forgot password/New here from 18 to 36 points, keeping the spacing within the secondary section unchanged.
- Verification: lint, TypeScript, 114 app tests and 43 Edge tests pass (dist/login-spacing-check.log).

## 2026-09-16 - L/R labels before Sets

- Moved side labels before the Sets column in ExerciseSetFields, used by workout entry and saved-workout editing. Regular exercises retain an empty matching gutter, keeping all data columns aligned. Shared row heights and synchronized rep scrolling are preserved.
- Existing browser regression verifies L/R and regular alignment, overflow focus and saved-workout edits; it passes. Reviewed the 320px screenshot. Lint, TypeScript, 114 app tests and 43 Edge tests pass. Logs: dist/side-label-position-check.log and dist/side-label-position-browser.log.

## 2026-09-16 - Restore regular exercise alignment

- User clarified that only unilateral exercises should retain the leading side-label gutter. Regular exercises now use the original Sets/x/Reps order, starting at the original left edge. The accepted L/R layout and synchronized rep scrolling remain intact in both workout entry and history editing.
- Existing alignment, overflow-focus, save and edit browser flow passes; reviewed the 320px screenshot. Lint, TypeScript, 114 app tests and 43 Edge tests pass. Logs: dist/regular-alignment-check.log and dist/regular-alignment-browser.log.

## 2026-09-17 - Add Food AI entry, adaptive appearance and smoother trends

- Moved the purple AI estimate entry into the Add Food method sheet while retaining the existing estimator design and consent/review workflow. Added a review-stage switch that defaults to saving reusable labels but can log the meal without creating My Foods profiles; repository persistence and later draft-amount edits preserve the opt-out.
- Displayed optional total package servings on provider suggestions, amount editing and meal-draft cards. Open Food Facts supplies package and serving measurements rather than a reliable direct servings count, so the existing resolver derives the value only when those measurements are compatible.
- Added persisted Device, Light and Dark controls to Profile, enabled automatic appearance in Expo configuration, applied iOS dynamic colors to shared screens/surfaces, and kept white foregrounds on accent actions. Web safely persists the preference without calling the native-only appearance override.
- Reworked shared confirmation actions into quieter bordered/soft-danger controls. Replaced straight weight and BP chart segments with monotone cubic paths that remain bounded by the measured values.
- Added unit coverage for appearance parsing, AI label opt-out and smooth paths, plus browser coverage for log-only AI meals and appearance selection. Full repository verification passes: lint, strict TypeScript, 118 app tests and 43 Edge tests; all 30 isolated Playwright flows pass. No database migration, hosted deployment or user-data mutation was required. Native iPhone appearance and layout review remains outstanding.

## 2026-09-17 - Calorie and fluid review (proposal only)

- User requested review and an implementation proposal before code changes. Added docs/CALORIE_FLUID_REVIEW.md with primary sources, exact Calculator.net activity options, numerical comparisons, current defects, proposed fluid-goal behavior, a comprehensive drink catalog and custom/Other/optional-AI logging design.
- Confirmed the existing DRI inactive example is approximately 641 kcal above Mifflin sedentary for the same test inputs; found a valid adult loss scenario producing a selectable 650 kcal/day goal. Recommended a separate lower-intake guard and preserving method assumptions.
- Distinguished beverage-volume credit from physiological hydration efficiency. Proposed full volume credit for nonalcoholic beverages, explicit separate alcohol tracking, pending unknown classifications, legacy policy snapshots and consistent aggregation across app surfaces. No unsupported fixed negative or caffeine-based hydration percentages are recommended.
- Verification: 10 existing calculator/hydration tests pass; independently reproduced formula and low-target examples. Calculator.net public HTML confirmed all activity factors. App code, database, stored user goals and hosted services were not changed; implementation is a planned roadmap item.

## 2026-09-17 - Calculator.net calories and manual drink categories

- Implemented the approved proposal excluding AI from drink logging. Calories now use Mifflin-St Jeor and the six Calculator.net factors, whole-kcal rounding, separate BMR display, minimum-intake eligibility and persisted calculation assumptions. Invalid recalculations clear prior selections.
- Added Suggested/Custom fluid modes, optional explicitly approximate exercise allowances and measured sweat-rate calibration. Custom goals bypass demographic requirements and preserve canonical volume. Saved goals do not update automatically.
- Added 35 categories, optional drink names, saved/recent drinks, pending unknown classification and per-entry reclassification. Nonalcoholic beverages count by consumed volume; alcohol is separate with no negative credit. Shared counting updates Summary, Water, History, daily totals, Coach and export. No AI calls or attachments are part of logging.
- Prepared migration 202609170002_drink_categories.sql for private saved drinks, versioned category/status snapshots, generated credit and goal provenance. Legacy records retain prior credit. Added transaction-rollback SQL fixtures for generated credit, category checks, reclassification and ownership policy isolation; execution awaits migration approval.
- Validation: lint, TypeScript, 138 app tests and 44 Edge tests pass; production iOS/Hermes export passes. All 35 browser flows pass across the full run and focused reruns after updating old test selectors/messages; reviewed synthetic 320px screenshots. Native iPhone verification remains pending. Logs: dist/fluid-calorie-check.log; bundle: apps/mobile/dist/ios-fluid-calorie/.
- Remote deployment was blocked by automatic approval review: implementation authorization was not accepted as deployment authorization for the linked backend. Dry-run identified only the intended migration. No migration or function deployment occurred and no alternate execution path was attempted. Explicit approval is required for this migration and the prepared coach-chat deployment; complete linked SQL/lint verification afterward.

## 2026-09-17 - Approved drink migration and Coach deployment

- User explicitly approved applying the drink-category migration and deploying the updated Coach to the linked Supabase backend, resolving the prior approval block. Rechecked the plan and applied its sole pending migration, 202609170002_drink_categories.sql.
- Linked SQL assertions pass for category constraints, generated goal credit, legacy totals, reclassification and saved-drink ownership isolation. Fixtures use temporary synthetic tables and roll back. Linked database lint reports no schema errors; final migration dry-run reports up to date.
- Deployed coach-chat through the Supabase API; version 12 is ACTIVE with JWT verification enabled. Endpoint smoke checks return OPTIONS 200 and unauthenticated POST 401. No model invocation or real health-record writes were used for verification.
- Application code is unchanged in this deployment pass, so the prior passing local suite and iOS export were not repeated. Authenticated live chat and physical-iPhone validation remain open; native verification is the active roadmap item.


## 2026-09-17 - Simple fluid goals and workout-specific AI planning

- Corrected floating-point weight-goal noise on calculator acceptance, repository read and save. Existing noisy values display cleanly without a bulk record rewrite; fractional values remain supported. Browser verification uses the reported 150 lb value.
- Replaced sweat measurements and exercise minutes with a sex/activity questionnaire and custom bypass. Converted the full drink category list into a searchable popup and removed My Drinks UI; no saved records were deleted. Recent drinks remain available without AI.
- Reordered the bottom bar to blank Soon placeholder / Summary / Track / History / Profile. Workout now launches a three-page questionnaire and specialized planner. Preferences separate setting/equipment from style and include optional auto-focus, goal, time, variety, experience, training frequency, readiness and limitations.
- Researched ACSM 2026, weekly set volume, RIR/failure and rest intervals. Documented evidence versus conservative app defaults in WORKOUT_PLANNER_EVIDENCE.md. Requests use only training tools/context, bounded dated history and 7/28-day primary-group volume; non-workout actions and unexpected tools are blocked. Plans require RIR/rest/technique, with beginner/readiness checks. Draft exercises preserve guidance; changing the movement clears its specific advice.
- Added exact-match ACE instructional/photo references for five movements and an explicit library fallback. No personal exercise catalog seeding, invented demo URLs or downloaded media. Exercise help is available in review and logging.
- Checks: lint, strict TypeScript, 139 app tests, 48 Edge tests, all 30 current browser flows across the full run and focused rerun, and production iOS/Hermes export pass. The browser rerun corrected one stale AI Coach tab label. Earlier focused testing caught/fixed transient duplicate category choices during popup dismissal and a test missing its synthetic backend fixture. A 320px planner screenshot was reviewed.
- Deployed coach-chat version 13 to linked HealthHub using the established authorized deployment workflow; ACTIVE with JWT verification enabled. OPTIONS returns 200 and unauthenticated POST returns 401. No database migration, live model invocation or real health-record writes were used in validation. Physical-iPhone and authenticated live-model quality remain open.

## 2026-09-18 - Dedicated Fluids history

- Split fluids from Food into a separate History tab, including URL selection and swipe navigation. Fluid cards retain date grouping, counted totals, separate pending/alcohol amounts, classification and confirmed deletion.
- Food history now groups meals only; its daily totals modal contains nutrition values without fluid rows. Empty states describe each category independently.
- Validation: TypeScript, targeted ESLint and all 11 selected existing browser flows pass across the full run and focused rerun. The rerun corrected a broad assertion matching hidden Water-screen content. Reviewed the 320px fluid-history screenshot; no backend changes or deployment were required. Physical-iPhone validation remains the active roadmap item.

## 2026-09-18 - Questionnaire directly populates workout and cardio drafts

- Removed chat UI and separate action-review flow from the planner. Generate now validates and fills the matching local drafts, then opens the existing editable log. Mixed sessions fill Lifting and Cardio; existing affected drafts require explicit replacement, while unrelated drafts survive. Draft population never writes completed history. Local-write retry retains the generated plan.
- Expanded goals/styles and added lifting/cardio/mixed choice, cardio activity, and required custom Other descriptions. No equipment is exclusive. Removed weekly-frequency question and science-based weekly-set blurb. Made history consent clearly selected with a plain description of draft creation.
- Retained bounded history, RIR/rest/technique and beginner checks. Added section-shape validation and a shared total-time feasibility guard. Existing legacy profile storage remains compatible; planning is for one session only.
- Validation: lint, TypeScript, 139 app tests, 50 Edge tests, four focused browser flows and iOS/Hermes export pass. Reviewed synthetic 320px screenshots. Initial local tests needed normal process-spawn escalation; one lint issue was corrected before the passing full check. No real health records or live model calls were used.
- Deployed coach-chat version 14 to the already authorized linked backend using CLI API bundling; confirmed ACTIVE, JWT verification true, OPTIONS 200 and unauthenticated POST 401. No database migration. Physical-iPhone and live model-quality validation remain pending.

## 2026-09-18 - Multi-select goals/styles and actionable planner validation

- Converted workout goals and styles to arrays with shared catalogs. Consolidated muscle/strength, endurance/conditioning, bodyweight/calisthenics, and intervals/HIIT. Normalized legacy single values and aliases at both client and API boundaries, preserving multiple choices and custom descriptions.
- Validated each questionnaire step, with inline field errors, navigation to the first affected step and scroll reset. Empty selections and incomplete Other fields cannot consume a model request. History-consent omission has a visible explanation. Saved nonstandard session times have visible selected chips.
- Added a compatibility default for the removed weekly-frequency profile number and nullish row handling before numeric coercion. A regression test reproduces an omitted legacy field and confirms the complete form can generate; the exact user-native failure is not independently reproduced. Profile saves and request errors now use actionable messages instead of raw missing-number/enum errors.
- Validation: lint, TypeScript, 139 app tests, 53 Edge tests and all five focused planner browser tests pass; reviewed the multi-select screenshot. Tests include backend rejection before quota/model use, legacy migration, plural preference transport and generation with missing legacy frequency. No paid model calls or real health-record writes.
- Deployed coach-chat version 15 through the already authorized API workflow; ACTIVE, JWT verification enabled, OPTIONS 200 and unauthenticated POST 401. No database migration. Prior iOS export was not repeated for this form/schema update; physical-device and live-model quality validation remain pending.

## 2026-09-18 - Collapsible history notes and consistent AI buttons

- Collapsed lifting/cardio History notes by default with independent accessible Show/Hide controls; full stored notes and editing are preserved.
- Simplified each fluid row to one consumed-volume value without duplicate goal-credit text. Kept classification/alcohol status and daily goal-counting behavior.
- Reused the existing meal-estimate card styling through a shared AiActionCard for meal estimation and workout planning, preserving their entry points and accessibility labels.
- Verification: TypeScript, targeted ESLint and all 16 existing review/drink/planner browser flows pass. Reviewed 320px workout-button and fluid-history screenshots. No new test cases, backend deployment or migration were required for these presentation changes; physical-iPhone validation remains pending.

## 2026-09-18 - Collapsed note previews

- Workout and cardio History notes retain a two-line preview with a trailing ellipsis. Show more/Show less toggles the full text, with matching accessible expanded state.
- Targeted ESLint and TypeScript pass. No new tests or backend changes for this small display refinement; the prior browser results remain recorded above.

## 2026-09-18 - Tighter fluid-history actions

- Removed the extra 14px top margin before each fluid-entry delete control; retained both 44px touch targets.
- Targeted ESLint and diff checks pass. No new tests or backend changes for this spacing adjustment. Reviewed all tracked/untracked work since c2e4a49 to provide a concise commit message; no commit was created.

## 2026-09-18 - Workout consent toggle and exercise descriptions

- Replaced the questionnaire's Checked/Enable consent chip with a labeled native Switch, preserving saved consent initialization, disabled state while busy and generation validation.
- Removed exercise-guide external links and browser messaging while retaining written cues, easier variations and general form guidance. Unmatched exercises now use a text-only unavailable-description message; original source metadata remains in the catalog.
- Updated existing browser selectors and guide expectations. TypeScript, targeted ESLint, diff checks and all four selected planner browser flows pass. Browser execution required sandbox escalation after spawn EPERM. No backend deployment or migration; physical-iPhone verification remains pending.

## 2026-09-18 - Blue workout-history toggle

- Changed the history-consent switch's on-state track to the app's blue theme color at the user's request. Diff check passes; browser tests were not repeated for this color-only adjustment.

## 2026-09-18 - Customizable Summary dashboard and streaks

- Reconciled the implementation prompt with HEAD `184c2a9`; preserved the existing uncommitted blue history toggle and text-only exercise guides. Navigation, workout planning, goal-calculator formulas, logging flows and native synchronization remain in their existing locations.
- Added focused Summary layout/registry/storage/rendering/data/training modules and streak model/engine/repository/UI. Existing Summary cards participate in the same layout; new cards expose completed training, fully paginated lifting PRs, draft-aware quick actions and all eleven optional streak types. Each chart owns its time-range state. Small/Wide layouts retain source order and adapt to narrow screens or large text.
- Implemented modal layout drafts, singleton/duplicate validation, gallery previews, resizing/settings, explicit handles with edge auto-scroll, non-drag reorder, protected cancellation/default restore, account-keyed serialized persistence and intentional empty layouts. Fixed a browser-discovered gesture bug where an adjacent handle could steal the active drag; background/privacy-lock cancellation releases the local scroll lock. Shortened editor action text without losing contextual accessible names.
- Added migration `202609180001_summary_streaks.sql` and SQL fixtures for dated rules/activation/goal snapshots and revision-fingerprinted food-day confirmation. Profile goal saving atomically preserves today and changes streak targets tomorrow; account-change guards protect delayed writes. Automatic protein stores resolved targets and weight provenance rather than using the newest weight retroactively. Nutrition mutations invalidate confirmation inside the transaction. Reminder schedule revisions stay user-scoped and local, with strict unknown coverage on unreadable records and conservative retention bounds.
- Domain tests cover current daily/weekly runs, unknown and scheduled periods, dated goals/schedules, DST/month/year boundaries, confirmation fingerprints/time-zone changes, inclusive calorie ranges, fluid policy/reclassification, manual/imported readings, matched BP pairs, occurrence-level reminders, workout shells/unilateral sets, PR baselines/ties/units/bodyweight/L-R/edits/deletes and history beyond the old caps. Storage tests cover corruption, empty layouts, sequential rapid writes, write failures, restart and account isolation.
- Verification: full lint/TypeScript/app/Edge lifecycle passes (156 app tests, 53 Edge tests). All 25 selected browser flows pass across full/focused runs; the final seven-flow editor/chart run includes held-edge auto-scroll and cancellation, and the drag regression additionally passed three consecutive runs. Isolated PostgreSQL migration/RLS fixtures pass via `node scripts/check-summary-sql.mjs`; the optional pinned runtime is confined to ignored `dist`. Production iOS/Hermes export passes. Reviewed 320px Summary and editor screenshots. Test execution required sandbox escalation for child processes; no real account records or paid model calls were used.
- Migration is **prepared, not deployed**. Linked-schema/RLS checks and physical-iPhone gesture/accessibility/privacy/Apple Health checks remain pending and are not established by browser tests or compilation. The updated Profile goal-save RPC requires the migration. Status/roadmap/ADR-008 document this boundary; the native-validation item remains the sole item in progress.

## 2026-09-18 - Approved hosted Summary verification and permission correction

- User requested application of the prepared migration. The linked HealthHub project was ACTIVE_HEALTHY and already recorded 202609180001; initial migration preview reported up to date, so the existing migration was not replayed.
- Hosted verification exposed a missing required meal_log_id in the synthetic fixture and direct anon EXECUTE grants inherited from hosted default privileges. Existing RPC authentication checks rejected unauthenticated calls, but the intended grant-level restriction was absent. Added 202609180002_summary_function_permissions.sql to revoke anonymous execution on all new Summary functions and direct client execution on the trigger function, retaining authenticated access to the four RPCs.
- Corrected the fixture, added explicit function-grant assertions and modeled hosted default grants plus required meal IDs in the isolated SQL runner. Isolated PostgreSQL verification passes. Reviewed the sole pending corrective migration in dry-run and applied it to HealthHub using the CLI.
- Hosted SQL/RLS assertions now pass for target snapshots, automatic protein stability, confirmation fingerprints/invalidation/undo, stale-account guards, account isolation and unauthenticated execution denial. Linked database lint reports no schema errors; final migration preview is up to date. Transactional synthetic accounts were confirmed absent after rollback. No real health records were used or changed by verification.
- Updated status, roadmap and ADR-008. App code was unchanged during deployment, so prior app/browser/iOS checks were not repeated. Physical-iPhone validation remains the sole in-progress roadmap item.

## 2026-09-18 - Native Edit Summary undefined-function fix

- Traced the opening failure to browser unload registration guarded only by window existence. React Native aliases window to its global object without DOM event methods. Restricted registration and cleanup to Platform.OS === "web" while preserving native navigation dismissal protection.
- Added a real Dashboard lifecycle test with native hosts and a window object without DOM methods. Before the fix it reproduces TypeError: window.addEventListener is not a function; afterward opening, canceling, resizing, saving and reopening all pass.
- Targeted ESLint and TypeScript pass. The existing browser editor flow passes add/reorder/resize, cancel/discard, save/reload and empty-layout persistence. No backend change was required. Native-style host tests do not replace physical-iPhone confirmation; that roadmap item remains in progress.

## 2026-09-18 - Live Summary editor and whole-widget dragging

- Replaced descriptor/settings cards with the same WidgetFrame and data renderers used by the saved Summary. Shared exact column widths and spacing keep previews faithful; tapping a card opens a separate size/configuration/removal panel. Gallery cards also show actual widget previews. Draft data requirements update while editing, and cancel restores the saved layout requirements.
- Restored the eight original widgets as the default: Weight, Blood pressure, Calories, Protein, Fluids, Calorie Calendar and both vital trends. Training, PR, Streaks and Quick Actions remain optional gallery additions. Existing saved custom layouts are preserved. UI sizes now read Small/Large; the persisted v1 wide value stays compatible.
- Added whole-widget long-press dragging, floating previews, stable insertion placeholders and bounded edge scrolling. Removed visible move buttons; retained screen-reader ordering actions. Native view/DOM order remains fixed until release so preview reflow cannot cancel pointer capture. On web, a nonpassive touch listener suppresses browser scrolling only during an active lifted drag. Ordinary swipes still scroll, and privacy/background/resize/dismissal clean up dragging.
- Verification: npm run check passes (159 app and 53 Edge tests, lint and TypeScript); final targeted native-style lifecycle/domain tests, ESLint and TypeScript pass. All nine Summary browser flows pass, including exact live-content/dimension comparisons, Small/Large save parity, mixed-size and adjacent reordering from widget bodies, ordinary touch scrolling and held vertical touch dragging, edge scrolling, cancellation, empty layouts and defaults. Production iOS/Hermes export passes. Browser screenshots cover 390px and 320px previews; native-device feel and VoiceOver remain pending.
- Logs: dist/summary-preview-check.log and apps/mobile/dist/summary-preview-ios.log. No database deployment or health-record changes were required. Physical-iPhone validation remains the sole in-progress roadmap item.

## 2026-09-18 - Prepared security, write consistency and multi-device hardening

- Confirmed the earlier review leads against current code, preserved the Summary/editor and workout UI work, and implemented server password proof plus user/device-bound, expiring, rotating and revocable biometric credentials. Added account/session lifecycle guards and Settings device/pending-save controls.
- Prepared migrations 202609180003-005: active-session validation, server versions, transactional workout session/sets, goal field comparisons, atomic per-account operation receipts, deleted-ID protection, commit-ordered vital sync, composite parent ownership and removal of unsafe old write grants. Read-only preflight reports violations; no automatic deletion or reassignment.
- Added durable client operation IDs, restart/lost-response recovery, completed receipts retained through draft clearing, atomic native SQLite/browser IndexedDB cache/outbox, dependency-aware acknowledgments, deletion-preserving remote shadows and explicit conflict recovery bound to the reviewed version. Complete history and totals pagination tolerates smaller API caps. Foreground refresh and account remounts preserve intended device-local drafts/reminders/layout.
- Validation: final lint/TypeScript and 171 app / 54 Edge helper tests pass; actual migrations and SQL permission/transaction/idempotency/cursor assertions pass in isolated PostgreSQL/PGlite. SQLite rollback, disk reopen, newer edits, tombstones and conflict-review races pass. All 24 selected browser flows pass across the full run and focused rerun; an early delete in the meal test was correctly rejected as stale, and the test now waits for the refreshed row (3/3 repeats). iOS/Hermes export passes.
- No production deployment, live health-record changes, or invented credentials. Staging real concurrent connections, hosted Auth/Edge/PostgREST/Storage, full Deno handler checking and physical-iPhone validation remain unverified. Photo bytes/metadata use separate transactions; receipt/tombstone retention is indefinite. Detailed file mapping, evidence, deployment/rollback order and limitations: docs/HARDENING.md. OTA remains disabled and unsafe older clients require a coordinated app upgrade.

## 2026-09-18 - Authorized backend rollout and partial History recovery

- User reported missing read_vital_changes and version columns. Read-only migration preview confirmed all three hardening migrations remained pending. Requested deployment approval because the earlier request explicitly prohibited production deployment; user approved the prepared update.
- Ownership preflight passed. Deployed biometric-auth v6 through the hosted API with its existing verify_jwt=false gateway setting for pre-session login, then applied 202609180003, 004 and 005. Refreshed PostgREST cache. Verified the exact RPC signature resolves and unauthenticated execution is denied; zero-row live API queries resolve workout_sessions, user_food_profiles and nutrition_entries version columns. No health entries were deleted/recreated; planned metadata backfill and credential revocation occurred.
- Hosted hardening SQL assertions and database lint pass. The first fixture run exposed an adapter mismatch: hosted auth.sessions.created_at requires explicit fixture initialization. Corrected only the fixture and reran successfully. All synthetic accounts rolled back, verified absent. Final migration preview is up to date; biometric-auth is ACTIVE v6 and rejects unauthenticated device listing.
- Changed History to apply successful category reads independently, retaining cached readings when workout or food reads fail. Added a clear pending-service-update message for missing functions/version columns. No legacy unsafe write fallback.
- Validation: lint, TypeScript, 172 app / 54 Edge tests, and all five hardening browser tests pass, including the partial-query failure regression. Logs: dist/backend-update-check.log and dist/backend-update-browser.log. Physical iPhone, authenticated Face ID/Storage integration and simultaneous separate PostgreSQL connections remain unverified.

## 2026-09-18 - Face ID exchange diagnostics and single-prompt rotation

- Reproduced generic HTTP 400 for older Face ID authenticate payloads; current invalid-credential requests return 401. Added native-compatible status/code extraction and prepared an explicit legacy-upgrade rejection, with helper tests. The Edge response improvement remains local; v6 is still deployed. User confirmed login works before reporting the second prompt.
- Verified the full deployed API exchange using a disposable account and managed keys held only in memory. Missing/wrong-password enrollment was rejected; correct enrollment, device binding, two rotated sessions, replay rejection and revocation passed. The synthetic account, sessions and credentials were removed; no health entries were written. Reproduction script: scripts/check-biometric-hosted.mjs, explicit opt-in required.
- Traced the duplicate iOS prompt to SecureStore get followed by updating the same requireAuthentication item. After server rotation/replacement invalidates the old secret, the client deletes the old item and creates a protected replacement, avoiding SecItemUpdate. Authentication and device-only/passcode protection remain; the new session is accepted only after successful protected persistence. Storage failure requires password recovery.
- Actual biometric-auth module adapter tests cover one prompt per login, later-login authentication, rotation/write/session order and write failure. Lint, TypeScript, 175 app / 55 Edge tests pass; log dist/face-id-rotation-check.log. Physical-iPhone prompt confirmation after Expo reload remains open. No new backend deployment or native dependency change was required for the duplicate-prompt fix.

## 2026-09-21 - Restore complete AI routines and workout-log handoff

- Traced sentence-only output to a generic model schema that permitted zero actions and validation that discarded an entire routine for one unfamiliar exercise with a non-null suggested load. Added a planner-specific nested result union requiring exactly one workout for normal safety and zero for caution/urgent. Verified the schema constraints against official documentation: https://developers.openai.com/api/docs/guides/structured-outputs . The app-facing response contract stays unchanged.
- Clear unsupported loads while retaining every exercise, mark new-to-history accurately, and report failed history queries. Missing/invalid/infeasible normal plans now fail before conversation persistence and refund their quota reservation. No automatic repair call or paid retry. Client treats an empty normal response as failure, preserving existing drafts; valid plans use the existing editable log handoff and replacement confirmation.
- Added regressions for normal/safety schema branches, complete/empty/discarded/infeasible model results, single-call/refund/save behavior, and mixed known/new exercise loads. Browser tests reproduce the reported sentence-only response, then verify a multi-exercise draft opens and remains editable. All five planner browser flows pass, as do lint, TypeScript, 175 app and 62 Edge tests (dist/workout-generation-check.log and dist/workout-generation-browser.log).
- Deployed only coach-chat (v16, existing JWT verification retained). One real hosted generation using a disposable synthetic account returned six fully specified exercises and persisted exactly one workout action. Cleanup removed the account and related planner records. Added explicit-opt-in scripts/check-workout-hosted.mjs; keys stay in memory, no credentials or model output are printed, and no personal health records are modified. Other deployed functions and migrations unchanged. Native iPhone handoff remains to be confirmed after reloading Expo.

## 2026-09-21 - Phone-first onboarding and account continuity

- Read the full onboarding brief and existing Auth/Face ID, goals, fluid, Profile, navigation and design components. Preserved previous uncommitted workout-generation changes. Removed the generated workout exercise-guide expansion while retaining technique/RIR/rest information and notes.
- Added native-style welcome, international phone selection/normalization, accessible single-input six-slot OTP verification, resend/loading/errors, and phone-first account entry. Email sign-in/signup/confirmation/recovery remain; signup names are optional. Added verified phone/email changes on the authenticated account with isolated verification and explicit user-ID checking, collision guidance, and verified-email recovery instructions. New UI contains no pets, gamification or fabricated legal links/results.
- Added resumable server setup plus optional preferred name/unit, goals, fluid, convenience and summary screens. Reused existing calculators and hardened goal persistence, retained canonical volumes, and did not create historical vital readings. Preferred names accept Unicode. New setup has maintain as its initial calculator intent; existing Profile behavior remains unchanged. Shared goal helpers now accept saved unit preferences. New country display names are bundled so phone entry does not depend on native Intl.DisplayNames support.
- Migration 202609210001_account_onboarding.sql backfills existing accounts complete and triggers incomplete rows for future accounts. RLS, active-session enforcement, row-version checks and operation receipts guard setup persistence. Per-user local drafts are checked against server baselines. Setup-provider tests cover account switches and stale reads after accepted writes. Existing app session persistence and the privacy lock remain, with onboarding also covered.
- Face ID enrollment now accepts either independently verified password or SMS OTP via Supabase, bound to the bearer account. Phone-only credentials enable current-session privacy locking, with truthful SMS-after-logout copy. No synthetic email, password replacement, custom session minting or reusable refresh-token storage was introduced to work around the email-only Admin generateLink API. A verified recovery email plus reenrollment supports the existing post-logout exchange.
- Deployed the reviewed onboarding migration and biometric-auth v7 (same pre-login gateway policy). Real hosted synthetic-account tests passed setup-trigger creation, completion, replay, stale conflict and direct/anonymous access rejection, then cleaned up. Existing password-backed Face ID hosted tests passed enrollment, device binding, two rotations, replay rejection and revocation, then cleaned up. Auth settings confirm phone provider disabled; actual SMS configuration/delivery remains external work.
- Verification: lint/TypeScript; 180 app / 64 Edge tests; 16 selected browser tests across onboarding, workout, existing goal and email/sync flows; isolated PostgreSQL migrations and hardening/onboarding assertions; iOS/Hermes export. Inspected rendered welcome, verification, completion and narrow-screen captures. Browser's existing adapter remains light even under a dark preference; native dark/keyboard/AutoFill/VoiceOver/Dynamic Type and phone-only biometric behavior need physical-device validation. Relevant logs: dist/onboarding-check.log, dist/onboarding-browser.log, dist/onboarding-final-browser.log, dist/onboarding-sql.log and dist/onboarding-ios.log. Detailed setup and limitations: docs/ONBOARDING.md.
- During verification, automatic approval review rejected a broad encoding-cleanup command because it could affect unrelated untracked files. Replaced it with explicitly named task-created files; no unrelated files were rewritten.

## 2026-09-21 - Email-primary onboarding; phone deferred

- Changed Welcome to primary email signup and existing-account email sign-in. Preserved confirmation/recovery, preferred name/units, goals, fluids, optional Face ID/reminders, saved summary and resumable account setup.
- Removed phone options from Settings/onboarding, changed optional Face ID enrollment to password reauthentication and retained the Profile password modal. Old phone links redirect to email sign-in. Existing accounts and server setup records stay intact; dormant SMS backend support is not exposed and phone auth remains disabled. No backend deployment is needed.
- Verification: lint/TypeScript, 180 app / 64 Edge tests, and nine selected browser cases pass. Eight browser cases passed initially; the ninth expected a heading that the existing sign-in UI does not render. Corrected it to the actual Sign in button and reran that case successfully. Inspected email welcome and saved-summary captures. Physical iPhone email links, Face ID, keyboard, VoiceOver and Dynamic Type still need device validation. Logs: dist/email-onboarding-check.log, dist/email-onboarding-browser.log and dist/email-onboarding-redirect.log.

## 2026-09-22 - Summary, meal labels, streaks and account deletion

- Preserved prior uncommitted work. Implemented the requested Summary/default/editor/gallery/settings design, PR-only layout migration and optional saved Today's Meals. Manual sync moved to Settings with automatic sync retained. Added derived meal-wide/mixed reusable-label switches and the login-first entry fallback without changing account-specific setup or authentication persistence.
- Added Done-only habit initialization, historical source evidence, daily imported/manual BP, accessible 1-7 training target with weekly progress, and inclusive confirmed calorie under/over rules. Preserved dated legacy calorie evidence, prospective settings and unknown historical targets.
- Added password-verified Edge deletion, storage-orphan enumeration, session/device revocation, guarded writes, idempotent cleanup and anonymous recovery receipts. Device recovery survives relaunch and clears account-scoped drafts, reminders, caches, pending writes and credentials only after verified server completion. Sealed unstarted tokens prevent delayed request races. No real user data deleted or deployment performed.
- Verification: lint/TypeScript, 184 app / 66 Edge tests, 30 browser cases, Deno check, all migrations plus hardening/onboarding/streak/deletion assertions in isolated PostgreSQL, and iOS/Hermes export. Reviewed synthetic screenshots. Browser touch tests now wait for modal positioning before sending coordinates; unknown-goal expectation matches the requested semantics. Updated older SQL fixtures to use active sessions and the existing hardened goal mutation. Logs and remaining hosted/native checks: docs/SUMMARY_REFINEMENTS.md.

## 2026-09-22 - Fix native streak text rendering error

- Found and removed an explicit whitespace string directly inside a Fragment beneath the streak Pressable, outside any Text host. Browser previews allowed it; native rendering reported an error for each initialized Large daily streak.
- Added an actual ExtraWidget render test using native hosts and the real streak engine for all 11 habits in both sizes; it rejects any string child of a non-Text host. Lint/TypeScript and 185 app / 66 Edge tests pass (dist/native-streak-text-check.log).
- Inspected the React Native animation event lifecycle. The reported onAnimatedValueUpdate warning cannot be reproduced/confirmed on this Windows environment; kept native animations and warnings intact pending iPhone recheck after reload. No backend changes required.

## 2026-09-22 - Deploy missing Summary streak RPC

- User reported schema-cache failure on Done after adding Streaks. Linked migration history confirmed 202609220001 was missing. Staged repository migrations only through that version, verified the dry run selected exactly one file, then applied it with the migration CLI and refreshed PostgREST. The unrelated account-deletion migration remains pending.
- Added scripts/check-streak-initialization-hosted.mjs. Disposable hosted-account verification passes the exact p_habits/p_zone request, all ten server habits, under-default/imported-BP defaults, repeat-call preservation and anonymous execution denial. Deleted the fixture and verified zero remaining owned streak records. No user account or health log was deleted or recreated. No client changes or native rebuild required.

## 2026-09-22 - Remove food-day completion at user request

- The existing control was under History > Food > View daily totals. User explicitly requested removing it and its streak option, superseding the earlier completion safeguards. Removed the control module, client confirmation mutation, queries/types/evaluation, option and associated instructions. Daily nutrition totals remain.
- Saved layouts strip food_complete from mixed widgets, remove retired-only widgets, preserve other configuration and serialize the migration. Retired server rules are ignored without deleting them or any health data. Calorie streaks now evaluate saved totals without manual confirmation, require nonempty logs and available numeric evidence, remain provisional for today and recalculate history after edits/deletions. Legacy numerical range/directional goals retain their dated configuration.
- Lint/TypeScript and 187 app / 66 Edge tests pass, including retirement persistence, existing rule compatibility, inclusive calorie targets, empty/missing evidence, edited totals and native-host rendering for all ten active habits. All 16 Summary browser cases pass, including legacy widgets/server rules, calorie progress with no confirmation, absent gallery option and totals without completion controls. Logs: dist/remove-food-completion-check.log and apps/mobile/dist/remove-food-completion-browser.log. No backend deployment needed.

## 2026-09-22 - Direct Streaks and Quick Actions editing

- Live widget headings now expose a small pencil with a 44-point target through WidgetFrame edit context. Context is absent from gallery/editor/settings previews. Direct editing opens the shared settings sheet without the full layout editor, with fixed Save/Cancel, save validation/errors and explicit discard. The shared save path initializes selected absent habits only when saving; other widget configuration remains intact. Direct removal stays staged until Save.
- Removed the preview's 210-point crop, added a Widget preview label and matched its width to Small/Large plus the accessible full-width fallback. The sheet scrolls while its optional footer remains fixed.
- Lint/TypeScript, 187 app / 66 Edge tests and 17 Summary browser cases pass. Direct-edit coverage verifies both shortcuts, save/discard, no initialization on preview/cancel, persistence, specific-widget isolation and an uncropped preview. Existing editor drag/save/restore and sheet/deletion tests also pass. Inspected final screenshot: dist/summary-refinements/direct-widget-options.png. Logs: dist/direct-widget-edit-check.log and apps/mobile/dist/direct-widget-edit-browser-final.log. Native presentation/accessibility still requires the iPhone; no backend deployment required.

## 2026-09-22 - Stop streak selection jitter

- Reproduced a 171-pixel row jump when deselecting a streak through its Summary pencil. The uncropped preview above the controls changed height as selected habits changed. Moved the full preview into a separate view of the same sheet, reached through Preview widget and dismissed with Back to options; save/discard still share the existing draft.
- Reused the instruction row for empty-selection validation and retained the recurring-reminder section with disabled choices when its streak is unselected, so toggles do not add/remove blocks from the scroll content.
- Browser regression checks identical row/sheet bounds and scroll offset at 390px and 320px through four selections, zero selections, reselection and reminder toggles. Failed before the fix; passes after. Lint/TypeScript, 187 app / 66 Edge tests and all 19 Summary browser cases pass. Inspected the updated synthetic direct-editor screenshot. Logs: dist/streak-jitter-check.log and apps/mobile/dist/streak-jitter-browser.log. Native iPhone verification remains pending; no backend changes or rebuild needed.

## 2026-09-22 - Training days minus icon

- Replaced the days-per-week slider decrement arrow with a horizontal minus matching the existing plus icon. Added minus to the shared SVG icon set; existing bounds, drag behavior and accessible button labels remain unchanged.
- Targeted ESLint and mobile TypeScript checks pass. No new tests for this icon-only change; native visual verification remains pending.

## 2026-09-22 - Shared current-week streak display

- Explained that Since described the best-streak history start, which differs by habit activation, available source dates and dated goal history. Removed that date from Summary cards while preserving it in details. Replaced the daily rolling date list and separate training row with one shared Monday-Sunday presentation for Small and Large cards: weekday initial, completion indicator and day number.
- Added a presentation helper mapping daily period states or training date evidence to the current local calendar week. Unknown, off-day, pre-activation and upcoming states remain distinct. Current/best totals and historical engine evaluation remain unchanged.
- Lint/TypeScript, 190 app / 66 Edge tests and five targeted browser cases pass. New date tests cover year boundaries, Sunday/Monday rollover, pre-activation/future days, unknown/off-days and training evidence. Existing native-host checks cover all ten habits in both sizes. Browser coverage verifies seven current-week dates, checks, no Since on cards, no horizontal overflow and existing editing/jitter behavior. Inspected synthetic Small/Large screenshots. Logs: dist/streak-week-check.log, apps/mobile/dist/streak-week-browser.log (initial new fixture missing user_id, corrected), and apps/mobile/dist/streak-week-visual.log. Native verification remains pending; no backend changes.

## 2026-09-22 - Goal-only streaks, combined sources and dated logging

- User removed logging streaks while keeping goals. Picker/defaults now offer calorie, protein, fluid and training targets only. Saved logging/reminder/food-completion selections are retired without deleting records or unrelated layout settings; active rule loading ignores retained retired server rules. Removed daily Met/progress copy while preserving calendar checks, training days/week and current/best counts.
- Diagnosed the weight best-streak discrepancy as a legacy manual-only import filter. Removed that gate even for older saved false preferences; manual and imported readings combine, with distinct-day evaluation preventing duplicate day credit. Added combined-source weight and nutrition-goal tests. Existing Apple Health import capabilities and permissions are unchanged.
- Added native and web Entry date controls to weight/BP, meals, fluids, lifting, cardio and progress-photo gallery uploads. Default is today at save time; selected dates use local calendar arithmetic, persist in applicable drafts/AI appends and participate in mutation identity. Occurrence timestamps, paired BP readings and photo dates use the chosen day. Successful entry forms reset to today. Existing hosted timestamp fields/transactions support this without a migration.
- Checks: lint/TypeScript, 195 app / 66 Edge tests, 11 Summary refinement browser cases, two dated-entry flows and the photo-upload case pass. Date tests cover DST/year boundaries, invalid/future dates, six save paths, retained meal/lifting/cardio dates across reload/sign-in, BP pairing, source mixing, photo dates and lost-response fluid retry without duplication. Browser testing caught and fixed a missing lifting date handoff. iOS/Hermes export passes; native picker/accessibility interaction remains pending. Logs: dist/goal-dates-check-final.log, apps/mobile/dist/goal-dates-browser.log (11 Summary cases passed; initial date fixture/navigation failures corrected in the next log), apps/mobile/dist/entry-dates-browser-final.log, apps/mobile/dist/goal-dates-photo-final.log and apps/mobile/dist/goal-dates-ios.log.

## 2026-09-22 - Consistent dated logging and fluid shortcuts

- Styled native/web Entry date with the shared calendar icon and tracking-input font, size, border and spacing. Removed the conditional native reset row so changing from today to a selected day keeps the closed field height stable; today remains selectable in the calendar. Web exposes an accessible calendar button while retaining the date input.
- Renamed Quick water amount to Quick fluid amount. Shortcuts set only the amount and matching fl-oz unit, preserving drink name/category/alcohol classification. New fluid forms start with no drink selected and require a category before saving. Updated existing water browser fixtures to explicitly select Water.
- Hoisted cardio state into a hook owned by the workout screen so both modes use the same date/AI/tabs header and retain independent drafts while switching. Scoped the unfinished lifting notice to its own tab.
- Verification: npm run check passes (lint, TypeScript, 195 app and 66 Edge tests). Ten relevant browser cases pass across date/draft, fluid classification, header coordinates, saves and retry/deletion coverage. The initial two new-test failures were incorrect test assumptions (tablist role and stored counted_ml); corrected to the actual accessible group and storage contract. Screenshots for fluid/date and both workout modes reviewed. iOS/Hermes export passes; no native interaction testing available. Logs: dist/logging-layout-check.log; apps/mobile/dist/logging-layout-browser.log, logging-layout-browser-final.log, logging-save-browser.log and logging-layout-ios.log.

## 2026-09-22 - First-launch welcome and deletion recovery repair

- Reproduced root entry always redirecting to sign-in with a permanent Back to welcome link. Added device-level welcome-seen storage, first-use routing and returning-user redirects; signup/sign-in mark the introduction seen independently of per-account setup. Unauthenticated onboarding routes to sign-in, and established account setup remains unchanged.
- Diagnosed deletion trap: the client persisted a secure pending capability before calling an undeployed function, then replaced all navigation regardless of auth/lock state. Added read-only readiness preflight, explicit missing-service error, bounded network wait, sign-out/return-to-login, explicit resume after relaunch, privacy-lock handling, and safe cancel only with a sealed server cancellation receipt. Ambiguous or started deletion remains recoverable. Fixed SettingsSheet double keyboard adjustment and nonshrinking viewport constraints; delete inputs use shared field styling.
- Live read-only inventory and db-push dry run confirmed only migration 202609220002 and delete-account were missing. Isolated full migration/SQL verification and Deno checks passed; applied only that migration. Auto-review rejected the exact function deployment because --no-verify-jwt changes gateway authentication on a destructive endpoint without explicit user approval. No alternative deployment/bypass was attempted. Function deployment and hosted deletion verification remain pending consent. Added a syntax-checked disposable-only hosted verification script for the approved follow-up; no real accounts were deleted.
- Verification: npm run check passes (195 app / 66 Edge tests), 22 relevant browser cases pass across onboarding, Summary sheets, deletion recovery and missing-function handling; native iOS/Hermes export passes. Compact-viewport browser checks approximate available keyboard space, not an actual iPhone keyboard. Early test runs caught the separate root redirect, an overly specific greeting assertion and a test adapter pulling navigation into the auth unit bundle; fixed. An older SQL harness lacked storage.owner_id; used the maintained onboarding/full-migration harness successfully. Logs: dist/deletion-recovery-check-final.log, deletion-recovery-sql-final.log, deletion-recovery-deno.log; apps/mobile/dist/deletion-onboarding-browser.log, deletion-recovery-browser.log, deletion-recovery-browser-final.log, deletion-dialog-visual.log and deletion-recovery-ios.log.

## 2026-09-22 - Approved account-deletion deployment

- User explicitly approved the previously blocked delete-account --no-verify-jwt deployment. Deployed the reviewed handler to linked HealthHub and confirmed ACTIVE version 1 with verify_jwt false; password/bearer verification for preparation and hashed recovery-capability verification for continuation remain in the handler. Linked database dry run reports no pending migrations.
- Ran scripts/check-account-deletion-hosted.mjs with explicit disposable-test opt-in. The initial fixture insert was correctly rejected by existing write hardening; changed the test to the app's commit_health_mutation RPC, then passed readiness, unauthorized/wrong-password rejection, old-session write rejection (28000), orphan-photo cleanup, Auth/profile/setup/fluid removal, recovery without a session, repeat completion, sealed cancellation and second-account isolation. All fixtures were removed on both runs. No real account or health record was targeted.
- Deployment requires no new native build. Old unstarted pending requests now receive a not_started/cancelled response and can clear safely. App-level keyboard/recovery/privacy changes still require the current bundle; physical-iPhone checks remain open. No application source changed this turn, so prior lint/TypeScript/unit/browser/iOS checks were not repeated. Evidence: dist/account-deletion-hosted.log (fixture failure), dist/account-deletion-hosted-final.log (pass), function inventory and linked dry-run output.


## 2026-09-22 - Persistent calorie/fluid settings flows

- Replaced the long helper UI with shared grouped controls and saved-result cards; retained Mifflin-St Jeor, activity multipliers, BMI/intake restrictions, gain percentages and beverage/activity/custom fluid formulas. Target Date computes exact local-calendar weeks and the required rate without substituting a standard option. Saved snapshots retain start/target dates, canonical dimensions, units and assumptions; explicit recalculation uses the current date.
- Extended existing profile JSON with helperVersion/latestCalculation while preserving top-level applied-goal provenance. New account-scoped AsyncStorage drafts are independently validated, ordered and conditionally cleared so a completing save cannot erase newer edits. Compatible legacy metadata restores without recomputation. Manual edits archive estimates. Profile/onboarding share state updates; custom fluids bypass Gender/activity.
- Failed-save checks exposed changing timestamps conflicting with durable mutation fingerprints. Matching retries now confirm the original operation and display the accepted snapshot; applying a result whose calculation response was lost first confirms that calculation. New input does not replace an uncertain earlier write. Explicit acceptance still controls daily numeric goals.
- Verification: npm run check (204 app / 66 Edge tests), 13 affected goal-helper/drink/onboarding browser flows and native iOS/Hermes export pass. Tested local-calendar DST/leap/year boundaries, invalid dates/rates/intakes, exact conversions, legacy input restoration, account-isolated drafts, reopen/tab/reload/sign-out, cancellation, HealthKit-weight opt-in, save failures, lost responses, apply retries and manual-goal preservation. Inspected 390px and 320px screenshots. Test iterations corrected fixture HealthKit shape/sync timing and an assertion to retain existing manual Profile rounding; production helper conversions remain exact.
- No migration, hosted deployment or new dependency. Actual iPhone checks remain for inline calendar interactions, keyboard avoidance, accessibility text scaling/VoiceOver, dark mode and process relaunch. Logs: dist/goal-helper-check.log and apps/mobile/dist/goal-helper-ios.log.


## 2026-09-22 - Grouped Profile and focused settings

- Replaced the large Profile/Settings toggle screen with a nested Profile stack, compact named header, five goal rows, preference/health-data/security groups, separate Sign Out and Manage Account, and version. Shared grouped rows, focused screen headers and short-edit sheets retain safe areas, reduced motion and accessible labels. The landing scroll position survives push/pop.
- Personal Details consolidates existing first/last/preferred name stores. Goal rows show actual saved values, Not Set, combined BP and the existing automatic protein rule; local cached weight refreshes after background sync. Manual editors patch only intentional fields via existing compare-and-set goal writes. Calculation snapshots and applied goals remain separate, using the existing persisted helpers. Unit edits preserve canonical quantities; unsaved name/contact/unit/goal changes have explicit discard handling.
- Health Data separates Apple Health from account synchronization, extracts pending saves/conflicts from device security, and adds a focused export screen with optional photo files, counts and size guidance. Reuses existing automatic synchronization, retries, archive contents and native sharing. Account screens reuse contacts, Face ID enrollment, server revocation, global sign-out confirmation and the deployed deletion flow, all under the existing privacy/auth boundaries.
- Browser appearance now uses the existing light/dark palettes through CSS variables; iOS retains DynamicColorIOS. Inspected synthetic 390/320px screenshots, dark appearance and enlarged browser text. Export checks open the generated archives and verify photo metadata in both variants and photo bytes only when selected. Mocked enrolled devices verify current-device labeling, revocation and global sign-out confirmation. No real account or health data was modified.
- Verification: root npm run check passes (lint, TypeScript, 207 app and 66 Edge tests); final iOS/Hermes export passes. The affected browser suites cover Profile, goals, drinks, retries/conflicts, login, onboarding/contacts, deletion recovery, Summary and general navigation. Updated obsolete toggle/global-save selectors, scoped modal/retained-stack labels and made an existing login-layout measurement wait for layout. One unrelated exercise-add timing failure passed unchanged on targeted rerun. All Profile tests passed on the final run.
- Evidence: dist/profile-check-final.log; apps/mobile/dist/profile-browser-final.log, profile-browser-regressions.log, profile-browser-recheck.log, profile-sync-navigation-final.log and profile-ios.log. Review screenshots in apps/mobile/dist/profile-redesign/. Native HealthKit permissions/import, Face ID prompts, share sheet, keyboard, Dynamic Type, VoiceOver and device cleanup still require the registered iPhone. No schema/dependency change or hosted deployment is needed.


## 2026-09-22 - Routine Reminders and coordinated notification lifecycle

- Reorganized Reminders into five opt-in routines and preserved custom reminders/completions. Added focused setup/category/quiet-hour sheets, weekday counts, native time pickers, independent meal/BP slots and editable fluid frequency. Denial retains choices and offers Settings without repeated permission prompts. New BP/weight custom creation suggests routines, while explicit structured overlap choices preserve history and delay replacement until confirmed scheduling.
- Replaced shared next-60 dated scheduling with serialized OS reconciliation, deterministic IDs/fingerprints, conservative 60-request budget plus four transition slots, custom-first verified migration and recoverable partial failure. Stable schedules repeat natively; workouts have a 28-local-day horizon supporting Rest Today, with 14-occurrence windows for exceptional custom start/completion semantics. Account-scoped preferences and device IDs are separate. Explicit logout/switch cancels routines only; deletion also cleans custom/legacy requests and pending navigation.
- Notification listener/startup capture now validates account-owned intents, deduplicates delivery responses, waits for auth/setup/privacy unlock, survives login retries and expires/cancels/consumes once. A final asynchronous account/lock check and matching-key expiry prevent late navigation races. Destinations use existing routes. Meal preselection preserves drafts and entry dates; taps and Rest Today create no activity/completion/streak evidence.
- Added deterministic native-adapter, scheduling, lifecycle, navigation and draft tests, including full legacy-window migration, capacity/partial failures, denial/re-enable, explicit logout/restore/switch/deletion, local-calendar DST/zone planning and no duplicate/orphan requests. Final root check passes: 231 app / 66 Edge tests, lint and TypeScript. All 30 affected browser cases and iOS/Hermes export pass. Initial browser failures were test selectors (setup switch intentionally stays off until activation; existing meal labels are lowercase), corrected after inspecting actual UI. Inspected synthetic light/dark 390/320px screenshots and enlarged browser text.
- ADR-009 documents Expo 57/Apple sources, current numeric-capacity uncertainty, local-time policy, bounded horizons and deferred saved-activity suppression. Actual iPhone delivery, suspended/cold-start taps, Face ID, OS DST/time-zone/calendar behavior, capacity, native pickers, Dynamic Type and VoiceOver remain pending. No real health/account data was changed; no migration, dependency or hosted deployment needed. Evidence: dist/routine-check.log; apps/mobile/dist/routine-model-tests-final.log, routine-browser-regressions.log, routine-ios.log and routine-reminders/ screenshots.


## 2026-09-22 - Onboarding keyboard and goal-helper alignment

- Investigated SetupFrame's keyboard-resizing container with a separate fixed two-action footer and the GoalHelper's own resizing wrapper. Moved onboarding actions into its bounded native keyboard-inset ScrollView, kept form content from shrinking, reset scrolling per step and dismiss editing before navigation. GoalHelper now uses native keyboard insets with stable header/tabs instead of a second resizing layout. An iOS InputAccessoryView scope supplies Done to shared inputs, including decimal keyboards; other screens keep their existing input behavior.
- Removed the inline fluid form and animated glass preview from onboarding. Saved daily goals and latest estimates are clearly separate; both calorie and fluid actions reuse Profile's persistent helper, including saved-result overview, edits, recalculation, Suggested/Custom, local-calendar loss planning and explicit application. Helper application syncs only intended fields so unfinished protein and target-weight entries survive. Updated convenience copy to Profile/Reminders.
- Root lint/TypeScript and 231 app / 66 Edge tests pass; iOS/Hermes export passes. Thirteen onboarding/shared-helper browser cases pass across the initial run and targeted recheck. New coverage verifies unapplied-result reuse, explicit application, unrelated manual-draft preservation, goal isolation, no fabricated readings and unchanged input dimensions/full visibility/actions at a 320x330 viewport. Initial data assertion raced the save; waiting for the next screen resolved it. Inspected dark compact screenshots; physical keyboard, Done accessory, page-sheet presentation, VoiceOver and Dynamic Type remain iPhone checks.
- Evidence: dist/onboarding-keyboard-check.log; apps/mobile/dist/onboarding-keyboard-browser.log, onboarding-keyboard-recheck.log, onboarding-keyboard-ios.log and onboarding-keyboard/. No dependency, formula, schema or hosted change. Exact Expo SDK 57 documentation and installed React Native input-accessory implementation were reviewed; native scrolling uses the documented automaticallyAdjustKeyboardInsets behavior.


## 2026-09-22 - Mysterious Coming Soon tab

- Replaced the empty Soon route with an original SVG sealed card, keyhole, redacted lines, violet orbital accents and Under Wraps / Coming Soon / In the Works copy. Reuses theme colors and ScreenScrollView, adapts artwork for short phones, and hides decorative SVG elements from assistive technology. Existing tab navigation is unchanged.
- Corrected a react-native-svg web transform warning and removed gradient references after resize screenshots exposed disappearing fills. Final lint/TypeScript, 231 app and 66 Edge tests pass. Existing navigation/accessibility browser review passes in light and dark at 320, 390, 430 and 1280px; inspected synthetic preview images. No new permanent tests for this static presentation change. Native device appearance remains pending with the existing iPhone validation item.
- Evidence: dist/coming-soon-check.log; apps/mobile/dist/coming-soon-browser.log and coming-soon-results/. No dependency, data, schema or deployment changes; reload the app bundle.


## 2026-09-23 - Reviewed application and SWE interview report

- Reconciled the supplied September 21 PDF against current routes, domain/repository modules, all migration paths, native adapters, ADR amendments and deployment records. Authored a 38-page report with per-feature implementation, reliability examples, alternatives, accepted costs, future scale plan, interview drafts and primary references. User-stated personal-use and career goals are explicit; inferred rationales are labeled retrospective.
- Fresh root check passes: 231 app and 66 Edge tests, lint and TypeScript. Isolated check-onboarding-sql passes all 36 migrations plus hardening/onboarding/streak/Summary/deletion assertions. All 55 selected browser regressions pass in one run. No hosted queries/deployment, paid AI request, device run or application change.
- Corrected prior missing-deletion, eleven-streak, food-confirmation, PR-widget, finite-only reminder, transient-helper and absent-export-version claims. Remaining export gaps, native/concurrency validation, local-storage caveats, history growth and operations are described without claiming public capacity.
- Generated PDF via local Chromium because Python/Poppler were unavailable; isolated PDF.js/canvas tools extracted and rendered both reports without altering app dependencies. Checked all 38 pages visually, verified all titles/text page count and zero content/footer overflow. Deliverables and a 414-file source hash inventory are under output/pdf; QA under dist/report-review-artifact-qa.


## 2026-09-23 - Independent reminders and medication/supplement schedules

- Removed the routine master toggle, quiet hours and their scheduling filter, per-day counts, Retry scheduling, device-local copy and Custom Reminders explanatory message. Five routines enable independently; a sixth Medication / Supplements entry manages multiple named reminders, each with its own switch, selected days, up to six times/day and an every-2-to-365-days option with start date. New custom reminders begin with a blank name and schedule controls.
- Existing medication/supplement IDs and history remain intact. Version-1 routine payloads migrate to version 2 without enabling master-off choices; chosen days/times are preserved. Interval scheduling counts calendar days across DST and uses a replenished 14-occurrence window with an in-editor refresh explanation. Existing capacity/error handling remains explicit. Every reminder editor uses a full-screen safe-area layout; time-card switches are vertically centered.
- Verification: full npm run check passes (235 app / 66 Edge tests plus lint/TypeScript); 35 focused reminder tests pass. All nine reminder browser cases and five related Profile cases pass across the initial run and corrected reminder rerun. Two browser assertions were corrected to reauthenticate after reload and wait for an overlap decision rather than expecting immediate activation. Inspected synthetic light and compact dark/enlarged-text screenshots. iOS/Hermes export passes; native keyboard/notification delivery still require the iPhone.
- Evidence: dist/reminders-redesign-check.log, reminders-redesign-unit.log; apps/mobile/dist/reminders-redesign-browser.log, reminders-redesign-browser-final.log, reminders-redesign-final-results/ and reminders-redesign-ios.log. ADR-009 has the superseding design details. No backend deployment or native dependency change; reload the development bundle.

## 2026-09-23 - Face ID enrollment restoration

- Reproduced the reported first-login failure with a synthetic email-only enrollment containing phone: "". The public account marker normalized that to an absent phone, while the protected credential compared the original empty string strictly; the reader returned no credential, sign-in showed the exact re-enable message, and the sign-in screen removed enrollment. Both readers now normalize absent/empty/null phones consistently, retain real-phone account binding and reject malformed phones. New protected writes and markers omit empty phones; intact older credentials work without re-enrollment.
- Unrelated gateway/session HTTP 401 failures now preserve the device credential; explicit invalid_device_credential and protocol-upgrade failures retain their intended recovery behavior. Ordinary local sign-out does not revoke Face ID. Background reauthentication remains deliberately opt-in through Face ID; Remember me stores account identity, while active Supabase sessions remain process-local. No new independent password-lock setting was introduced.
- Validation: captured the failing regression in apps/mobile/dist/face-id-reproduction.log, then passed all 13 targeted credential/storage/lifecycle/error tests. Full npm run check passes (238 app / 66 Edge tests plus lint/TypeScript), all seven login/Profile browser regressions pass, and iOS/Hermes export passes. The synthetic native harness covers legacy empty-phone reads, enrollment, fresh-module restoration, repeated authenticated rotation and fail-closed persistence; provider checks cover background locking and local sign-out without revocation. Physical Keychain/Face ID prompts still require the iPhone.
- Evidence: dist/face-id-fix-check.log; apps/mobile/dist/face-id-fix-unit.log, face-id-fix-browser.log and face-id-fix-ios.log. No server/dependency/native binary change is required; reload the updated development bundle. If a previous failed attempt already deleted the saved credential, sign in with the password and enable Face ID once more.

## 2026-09-23 - Reminder and Profile refinements

- Removed Reminders from Profile Preferences; Track retains the entry point and the old Profile URL redirects to the shared destination. Removed the Routine Reminders subheader and scheduled-success paragraph. The Custom Reminders add button now sits directly inside its section below the heading/list.
- Medication / Supplements uses the same RoutineRow icon, label, summary, chevron and centered switch as the five other reminders. Its persisted medicationEnabled preference pauses/resumes effective schedules without overwriting individual enabled flags, times or completion history; missing legacy fields default to enabled to preserve existing delivery. Tapping the row opens item setup, and enabling an empty group opens setup too. Cancellation still works while notification permission is blocked, and other custom reminders remain active.
- Reminder deletion now uses a centered overlay and shared Cancel/Delete actions above the retained editor. Cancel keeps unsaved inputs; failure remains in the popup. Covered background editor controls are hidden from accessibility. The shared Profile row keeps label/value on one row while allowing their text to wrap, vertically centering Up to Date alongside the long sync label.
- Verification: npm run check passes (238 app / 66 Edge tests plus lint/TypeScript); all 35 reminder unit tests pass, including group pause/resume, individual-choice preservation and blocked-permission cancellation. Seventeen browser cases pass across the initial run and targeted reruns (10 reminder, 6 Profile, 1 existing navigation flow). Updated obsolete Profile-link expectations and status-text casing; geometry assertions wait for loaded status and measure both texts atomically. Inspected synthetic reminder/delete/390px and 320px Profile screenshots. iOS/Hermes export passes; actual iPhone delivery and accessibility remain device checks.
- Evidence: dist/reminder-polish-check.log; apps/mobile/dist/reminder-polish-unit.log, reminder-polish-browser.log, reminder-polish-profile-final.log, reminder-polish-alignment.log, reminder-polish-navigation.log and reminder-polish-ios.log. No backend deployment or native dependency change; reload the app bundle.

## 2026-09-23 - Reminder modal continuity and delete spacing

- Matched custom and medication/supplement delete cards to the existing saved-food popup: 20px card padding, 19px title, 8px body gap, and shared equal Cancel/Delete actions. Removed inherited paragraph bottom spacing and the Add Custom Reminder button's bottom margin so its label is centered in the visible section.
- Added embedded content support to SettingsSheet and retained one full-screen modal across the medication list and editor. Add/edit/save/cancel/delete no longer dismiss one native modal before opening another. Forwarded hardware-back/escape through the editor's existing busy, delete-confirmation and unsaved-change guards.
- Root lint/TypeScript and all 238 app / 66 Edge tests pass. All 17 reminder/Profile browser cases pass, including persistent modal identity, save/cancel/delete navigation, unsaved-edit retention and measured label centering. Visually inspected both delete popup screenshots. Final iOS/Hermes export passes; actual iPhone modal animation, keyboard and accessibility still require the device.
- Evidence: dist/reminder-modal-check.log; apps/mobile/dist/reminder-modal-browser.log, reminder-modal-results/ and reminder-modal-ios.log. No backend or native dependency change; reload the bundle.

## 2026-09-23 - Calorie calendar fill animation

- Replaced static daily calorie arcs with 650ms clockwise fills: Animated SVG stroke offsets on native and CSS SVG stroke transitions on web. Stable rings animate subsequent totals/goal changes; date keys reset rings when months change. Reduce Motion shows the target immediately, and effects clean up when interrupted/unmounted.
- Kept existing capped progress, over-goal colors, no-goal presentation and calorie labels. Initial totals wait for the Summary goal load so partial data cannot briefly show a full no-goal ring before the correct calorie fraction.
- Verification: root lint/TypeScript and 238 app / 66 Edge tests pass; all nine existing Summary browser cases pass. Final TypeScript and iOS/Hermes export pass. Actual animation smoothness remains a physical-iPhone check. Exact Expo SDK 57 documentation reviewed; no new dependencies, schema or backend deployment.
- Evidence: dist/calorie-calendar-check.log, calorie-calendar-typecheck.log; apps/mobile/dist/calorie-calendar-browser.log, calorie-calendar-results/ and calorie-calendar-ios.log.

## 2026-09-23 - Sustain login, launch and secure app entry

- Read the supplied brief and reviewed existing authentication, secure Face ID exchange, privacy boundaries, account deletion, onboarding, notification routing and motion/theme code. Extracted exact symbol/wordmark crops from the supplied transparent logo board and an alpha mask, retaining the source asset and crop provenance. No replacement letterform was generated.
- Added calm light/dark login and lock branding, a contained pending highlight, a stationary wordmark, guarded 520ms manual / 320ms biometric success transitions and a once-per-attempt light native haptic. Reduce Motion disables animation/haptic. Manual success retains the form's logo placement. Login now uses the existing native keyboard-inset scrolling pattern and a stable labeled busy button.
- EntryProvider coordinates presentation around the existing auth/privacy/setup authorities. Numbered attempts reject stale completion, duplicate submission is blocked synchronously, and an opaque cover stays above prepared destinations until access and navigation are ready. Critical reads/navigation expose retry/sign-out; unrelated health synchronization is not awaited. Session-security reads survive token refresh, and late biometric success is bound to the requesting session.
- Integrated destination selection with the existing reminder observer: startup capture settles first; tap capture is ordered; account/expiry/allowlist/deduplication and draft protection are retained. Sign-in and onboarding completion resolve directly to valid loggers without mounting Summary, and normal unlocked taps replace rather than stack destinations. Opening a reminder does not create or backdate a health entry.
- Added Expo SDK 57 splash-screen/haptics modules and matching native/React launch presentation. The native splash uses system appearance captured before the saved in-app preference is applied. A new iOS binary is required; no backend deployment, bundle identifier, auth callback scheme or session-persistence protocol change.
- Verification: lint/TypeScript and all 240 app / 66 Edge tests pass. All 36 affected auth/onboarding/reminder/deletion/hardening browser cases pass across the main run and targeted reruns; five entry-specific cases also verify the final presentation. Corrected an unsupported SVG web attribute and obsolete branding/retry selectors. Two interaction assertions now wait for the covered destination to become interactive before inspecting/typing; draft preservation passes. Inspected synthetic light/dark compact screenshots. iOS/Hermes export and native-config introspection pass. Physical Face ID, native notification cold starts, VoiceOver, keyboard and standalone splash/haptic behavior remain device checks.
- Evidence: dist/sustain-final-check.log; apps/mobile/dist/sustain-final-browser.log, sustain-recheck-browser.log, sustain-polish-browser.log, sustain-ios.log and sustain-native-config.json. ADR-010 documents the state and routing tradeoffs. Reviewed the full accumulated change list for the user-authorized commit/push; generated QA logs/screenshots and environment files remain ignored, and the approved artwork plus previously requested report are included.
