# Dependency patches

No active patches. The former draggable-list patch was removed with exercise dragging; workouts now use native FlatList rendering and up/down actions. The root postinstall remains ready to apply future reviewed patches.
